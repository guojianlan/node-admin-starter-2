# Admin Base 页面验收测试矩阵

> 该文件由 `pnpm test:docs` 根据 `tests/coverage/page-test-cases.ts` 生成。请修改机器可读清单，不要直接维护本文件。

## 1. 范围与判定

- 当前登记 App Router 页面：**36** 个。
- 数据验收覆盖成功、加载、空数据和错误四种状态。
- 交互验收覆盖成功、表单/网络/业务失败以及页面和动作权限。
- 视觉验收覆盖桌面、窄屏、浅色和暗色；视觉成功要求无重叠、无意外溢出、可读层级清楚、固定区域稳定。
- 视觉失败包括但不限于：表格底边框缺失、内容区未撑满、固定头尾遮挡、暗色硬编码白底、侧栏文字溢出、弹层截断、loading 引发布局跳动。
- 视觉用例需要截图或录屏证据；数据用例需要 API 响应、数据库断言或 requestId 证据。

## 2. 页面索引

| ID | Route | 模块 | 覆盖 |
| --- | --- | --- | --- |
| PAGE-001 | `/` | 入口路由 | `automated` |
| PAGE-002 | `/login` | 认证 | `mixed` |
| PAGE-003 | `/dashboard` | 系统状态 | `mixed` |
| PAGE-004 | `/profile` | 个人中心 | `mixed` |
| PAGE-005 | `/system/user` | 用户管理 | `mixed` |
| PAGE-006 | `/system/role` | 角色权限 | `mixed` |
| PAGE-007 | `/system/rule` | 菜单权限 | `mixed` |
| PAGE-008 | `/system/dept` | 组织架构 | `mixed` |
| PAGE-009 | `/system/dict` | 字典管理 | `mixed` |
| PAGE-010 | `/system/dict/item` | 字典管理 | `mixed` |
| PAGE-011 | `/system/config` | 配置项维护 | `mixed` |
| PAGE-012 | `/system/settings` | 系统设置 | `mixed` |
| PAGE-013 | `/system/file` | 文件管理 | `mixed` |
| PAGE-014 | `/system/storage` | 存储配置 | `mixed` |
| PAGE-015 | `/system/mail/account` | 邮件配置 | `mixed` |
| PAGE-016 | `/system/oauth/provider` | 第三方登录 | `mixed` |
| PAGE-017 | `/system/sms/provider` | 短信配置 | `mixed` |
| PAGE-018 | `/system/sms/template` | 短信模板 | `mixed` |
| PAGE-019 | `/system/notice` | 通知公告 | `mixed` |
| PAGE-020 | `/system/login/log` | 登录审计 | `mixed` |
| PAGE-021 | `/system/online/user` | 在线会话 | `mixed` |
| PAGE-022 | `/system/operation/log` | 操作审计 | `mixed` |
| PAGE-023 | `/system/ai/setup` | AI 接入 | `mixed` |
| PAGE-024 | `/system/ai/provider` | AI 服务商 | `mixed` |
| PAGE-025 | `/system/ai/model` | 模型管理 | `mixed` |
| PAGE-026 | `/system/ai/playground` | AI Playground | `mixed` |
| PAGE-027 | `/system/ai/runtime` | AI 运行治理 | `automated` |
| PAGE-028 | `/system/ai/knowledge` | Knowledge/RAG | `automated` |
| PAGE-029 | `/system/ai/notebook` | AI Notebook | `automated` |
| PAGE-030 | `/system/ai/eval` | AI Eval / Trace | `automated` |
| PAGE-031 | `/system/ai/governance` | AI 治理 | `automated` |
| PAGE-032 | `/system/ai/chat` | AI Chat | `mixed` |
| PAGE-033 | `/system/ai/agent` | AI Agent | `mixed` |
| PAGE-034 | `/system/ai/workflow` | AI Workflow | `mixed` |
| PAGE-035 | `/system/ai/web-search` | 联网搜索 | `mixed` |
| PAGE-036 | `/system/module/generator` | 模块生成器 | `mixed` |

## 3. 逐页面验收标准

### PAGE-001 `/`

- 模块：入口路由
- 数据成功：根路由不承载业务数据并按登录状态进入正确目标页使用真实接口数据，字段、总数、排序、筛选和刷新结果一致
- Loading：首次加载和刷新期间显示稳定 loading，不闪现旧数据、不改变主布局尺寸
- Empty：该路由无空数据态；未登录进入登录页，已登录进入 Dashboard
- Error：接口失败时显示可读错误和重试入口；旧数据不伪装成最新成功结果
- 交互成功：访问根地址完成后给出明确反馈，列表、详情、URL 和缓存状态同步
- 交互失败：表单校验、业务约束或网络失败时保留用户输入，关闭重复提交并显示准确原因
- 权限：跳转不能绕过登录和强制改密守卫
- Desktop：跳转过程不展示破碎 Shell、不产生明显布局闪烁或重复导航；主工作区填满 Shell 分配的可用高度，页面头部保持稳定，表格、Tabs、树或专用画布在内容区内部滚动，底部不出现因自然高度收缩造成的无意义空白
- Narrow：窄屏下无控件重叠和横向页面溢出；复杂表格允许容器内横向滚动，关键操作仍可触达
- Light：浅色主题下背景、边框、正文、次要文字、选中态和危险操作达到清晰层级与可读对比
- Dark：暗色主题不出现硬编码白底/黑字；表格、弹窗、抽屉、编辑器、图表和 hover 状态均可辨识
- 当前覆盖：`automated`

### PAGE-002 `/login`

- 模块：认证
- 数据成功：登录策略、验证码开关和 OAuth Provider 入口使用真实接口数据，字段、总数、排序、筛选和刷新结果一致
- Loading：首次加载和刷新期间显示稳定 loading，不闪现旧数据、不改变主布局尺寸
- Empty：没有可用 OAuth Provider 时仅隐藏第三方入口，账号密码登录保持完整
- Error：接口失败时显示可读错误和重试入口；旧数据不伪装成最新成功结果
- 交互成功：密码登录、验证码刷新、忘记密码和 OAuth 跳转完成后给出明确反馈，列表、详情、URL 和缓存状态同步
- 交互失败：表单校验、业务约束或网络失败时保留用户输入，关闭重复提交并显示准确原因
- 权限：登录页允许匿名访问；已登录用户访问时按产品策略跳转后台
- Desktop：登录表单在首屏视觉居中，Logo、标题、输入框、主按钮和辅助入口层级清楚且无装饰性干扰；主工作区填满 Shell 分配的可用高度，页面头部保持稳定，表格、Tabs、树或专用画布在内容区内部滚动，底部不出现因自然高度收缩造成的无意义空白
- Narrow：窄屏下无控件重叠和横向页面溢出；复杂表格允许容器内横向滚动，关键操作仍可触达
- Light：浅色主题下背景、边框、正文、次要文字、选中态和危险操作达到清晰层级与可读对比
- Dark：暗色主题不出现硬编码白底/黑字；表格、弹窗、抽屉、编辑器、图表和 hover 状态均可辨识
- 当前覆盖：`mixed`

### PAGE-003 `/dashboard`

- 模块：系统状态
- 数据成功：登录、在线用户、操作日志、存储、邮件、公告和 doctor 摘要指标使用真实接口数据，字段、总数、排序、筛选和刷新结果一致
- Loading：首次加载和刷新期间显示稳定 loading，不闪现旧数据、不改变主布局尺寸
- Empty：无记录时显示明确空状态，保留可用的新增、刷新或返回动作
- Error：接口失败时显示可读错误和重试入口；旧数据不伪装成最新成功结果
- 交互成功：查看指标并跳转对应管理页面或公告详情完成后给出明确反馈，列表、详情、URL 和缓存状态同步
- 交互失败：表单校验、业务约束或网络失败时保留用户输入，关闭重复提交并显示准确原因
- 权限：只展示当前用户有权访问模块的指标和跳转入口
- Desktop：指标密度适合运维后台，卡片高度稳定，图表与状态列表对齐且不使用营销大屏构图；主工作区填满 Shell 分配的可用高度，页面头部保持稳定，表格、Tabs、树或专用画布在内容区内部滚动，底部不出现因自然高度收缩造成的无意义空白
- Narrow：窄屏下无控件重叠和横向页面溢出；复杂表格允许容器内横向滚动，关键操作仍可触达
- Light：浅色主题下背景、边框、正文、次要文字、选中态和危险操作达到清晰层级与可读对比
- Dark：暗色主题不出现硬编码白底/黑字；表格、弹窗、抽屉、编辑器、图表和 hover 状态均可辨识
- 当前覆盖：`mixed`

### PAGE-004 `/profile`

- 模块：个人中心
- 数据成功：个人资料、头像、密码、登录记录和 OAuth 绑定使用真实接口数据，字段、总数、排序、筛选和刷新结果一致
- Loading：首次加载和刷新期间显示稳定 loading，不闪现旧数据、不改变主布局尺寸
- Empty：无记录时显示明确空状态，保留可用的新增、刷新或返回动作
- Error：接口失败时显示可读错误和重试入口；旧数据不伪装成最新成功结果
- 交互成功：更新资料、上传头像、修改密码和绑定/解绑第三方账号完成后给出明确反馈，列表、详情、URL 和缓存状态同步
- 交互失败：表单校验、业务约束或网络失败时保留用户输入，关闭重复提交并显示准确原因
- 权限：仅能读取和修改当前用户；强制改密期间该页和退出登录仍可使用
- Desktop：资料导航与编辑区比例稳定，头像、表单、历史记录和安全动作形成清楚分组，不使用卡片嵌套；主工作区填满 Shell 分配的可用高度，页面头部保持稳定，表格、Tabs、树或专用画布在内容区内部滚动，底部不出现因自然高度收缩造成的无意义空白
- Narrow：窄屏下无控件重叠和横向页面溢出；复杂表格允许容器内横向滚动，关键操作仍可触达
- Light：浅色主题下背景、边框、正文、次要文字、选中态和危险操作达到清晰层级与可读对比
- Dark：暗色主题不出现硬编码白底/黑字；表格、弹窗、抽屉、编辑器、图表和 hover 状态均可辨识
- 当前覆盖：`mixed`

### PAGE-005 `/system/user`

- 模块：用户管理
- 数据成功：用户分页、部门树、角色、状态和搜索条件使用真实接口数据，字段、总数、排序、筛选和刷新结果一致
- Loading：首次加载和刷新期间显示稳定 loading，不闪现旧数据、不改变主布局尺寸
- Empty：无记录时显示明确空状态，保留可用的新增、刷新或返回动作
- Error：接口失败时显示可读错误和重试入口；旧数据不伪装成最新成功结果
- 交互成功：新增、编辑、删除、重置密码、分配角色和切换部门筛选完成后给出明确反馈，列表、详情、URL 和缓存状态同步
- 交互失败：表单校验、业务约束或网络失败时保留用户输入，关闭重复提交并显示准确原因
- 权限：无页面权限时菜单不可见且直接访问被拒绝；无动作权限时对应按钮不可见或禁用
- Desktop：部门树与主表格撑满可用高度；用户名、昵称、部门、角色、状态和操作列宽合理，表头/分页固定且底边框完整；主工作区填满 Shell 分配的可用高度，页面头部保持稳定，表格、Tabs、树或专用画布在内容区内部滚动，底部不出现因自然高度收缩造成的无意义空白
- Narrow：窄屏下无控件重叠和横向页面溢出；复杂表格允许容器内横向滚动，关键操作仍可触达
- Light：浅色主题下背景、边框、正文、次要文字、选中态和危险操作达到清晰层级与可读对比
- Dark：暗色主题不出现硬编码白底/黑字；表格、弹窗、抽屉、编辑器、图表和 hover 状态均可辨识
- 当前覆盖：`mixed`

### PAGE-006 `/system/role`

- 模块：角色权限
- 数据成功：角色列表、成员、菜单权限和数据范围使用真实接口数据，字段、总数、排序、筛选和刷新结果一致
- Loading：首次加载和刷新期间显示稳定 loading，不闪现旧数据、不改变主布局尺寸
- Empty：无记录时显示明确空状态，保留可用的新增、刷新或返回动作
- Error：接口失败时显示可读错误和重试入口；旧数据不伪装成最新成功结果
- 交互成功：新增、编辑、复制角色、预览授权差异并保存权限完成后给出明确反馈，列表、详情、URL 和缓存状态同步
- 交互失败：表单校验、业务约束或网络失败时保留用户输入，关闭重复提交并显示准确原因
- 权限：无页面权限时菜单不可见且直接访问被拒绝；无动作权限时对应按钮不可见或禁用
- Desktop：角色表格、权限树和差异确认抽屉使用完整容器高度；长权限名称可读，危险权限有视觉标识；主工作区填满 Shell 分配的可用高度，页面头部保持稳定，表格、Tabs、树或专用画布在内容区内部滚动，底部不出现因自然高度收缩造成的无意义空白
- Narrow：窄屏下无控件重叠和横向页面溢出；复杂表格允许容器内横向滚动，关键操作仍可触达
- Light：浅色主题下背景、边框、正文、次要文字、选中态和危险操作达到清晰层级与可读对比
- Dark：暗色主题不出现硬编码白底/黑字；表格、弹窗、抽屉、编辑器、图表和 hover 状态均可辨识
- 当前覆盖：`mixed`

### PAGE-007 `/system/rule`

- 模块：菜单权限
- 数据成功：菜单、路由、目录和动作权限树使用真实接口数据，字段、总数、排序、筛选和刷新结果一致
- Loading：首次加载和刷新期间显示稳定 loading，不闪现旧数据、不改变主布局尺寸
- Empty：无记录时显示明确空状态，保留可用的新增、刷新或返回动作
- Error：接口失败时显示可读错误和重试入口；旧数据不伪装成最新成功结果
- 交互成功：新增、编辑、显隐、启停和删除权限节点完成后给出明确反馈，列表、详情、URL 和缓存状态同步
- 交互失败：表单校验、业务约束或网络失败时保留用户输入，关闭重复提交并显示准确原因
- 权限：无页面权限时菜单不可见且直接访问被拒绝；无动作权限时对应按钮不可见或禁用
- Desktop：树形表格撑满内容区，不设置无理由的较小 max-height；层级缩进、展开控件、固定操作列和底边框清晰；主工作区填满 Shell 分配的可用高度，页面头部保持稳定，表格、Tabs、树或专用画布在内容区内部滚动，底部不出现因自然高度收缩造成的无意义空白
- Narrow：窄屏下无控件重叠和横向页面溢出；复杂表格允许容器内横向滚动，关键操作仍可触达
- Light：浅色主题下背景、边框、正文、次要文字、选中态和危险操作达到清晰层级与可读对比
- Dark：暗色主题不出现硬编码白底/黑字；表格、弹窗、抽屉、编辑器、图表和 hover 状态均可辨识
- 当前覆盖：`mixed`

### PAGE-008 `/system/dept`

- 模块：组织架构
- 数据成功：部门树、负责人、状态和部门用户使用真实接口数据，字段、总数、排序、筛选和刷新结果一致
- Loading：首次加载和刷新期间显示稳定 loading，不闪现旧数据、不改变主布局尺寸
- Empty：无记录时显示明确空状态，保留可用的新增、刷新或返回动作
- Error：接口失败时显示可读错误和重试入口；旧数据不伪装成最新成功结果
- 交互成功：新增子部门、编辑、删除和查看部门用户完成后给出明确反馈，列表、详情、URL 和缓存状态同步
- 交互失败：表单校验、业务约束或网络失败时保留用户输入，关闭重复提交并显示准确原因
- 权限：无页面权限时菜单不可见且直接访问被拒绝；无动作权限时对应按钮不可见或禁用
- Desktop：树表层级与操作列平衡，长部门名不挤压状态和操作，表格占满内容区并保留底部边界；主工作区填满 Shell 分配的可用高度，页面头部保持稳定，表格、Tabs、树或专用画布在内容区内部滚动，底部不出现因自然高度收缩造成的无意义空白
- Narrow：窄屏下无控件重叠和横向页面溢出；复杂表格允许容器内横向滚动，关键操作仍可触达
- Light：浅色主题下背景、边框、正文、次要文字、选中态和危险操作达到清晰层级与可读对比
- Dark：暗色主题不出现硬编码白底/黑字；表格、弹窗、抽屉、编辑器、图表和 hover 状态均可辨识
- 当前覆盖：`mixed`

### PAGE-009 `/system/dict`

- 模块：字典管理
- 数据成功：字典类型、编码、状态和关联字典项使用真实接口数据，字段、总数、排序、筛选和刷新结果一致
- Loading：首次加载和刷新期间显示稳定 loading，不闪现旧数据、不改变主布局尺寸
- Empty：无记录时显示明确空状态，保留可用的新增、刷新或返回动作
- Error：接口失败时显示可读错误和重试入口；旧数据不伪装成最新成功结果
- 交互成功：新增、编辑、删除字典并进入字典项完成后给出明确反馈，列表、详情、URL 和缓存状态同步
- 交互失败：表单校验、业务约束或网络失败时保留用户输入，关闭重复提交并显示准确原因
- 权限：无页面权限时菜单不可见且直接访问被拒绝；无动作权限时对应按钮不可见或禁用
- Desktop：搜索、主操作、表格和分页形成单一工作流；编码列可复制，状态与操作列宽稳定；主工作区填满 Shell 分配的可用高度，页面头部保持稳定，表格、Tabs、树或专用画布在内容区内部滚动，底部不出现因自然高度收缩造成的无意义空白
- Narrow：窄屏下无控件重叠和横向页面溢出；复杂表格允许容器内横向滚动，关键操作仍可触达
- Light：浅色主题下背景、边框、正文、次要文字、选中态和危险操作达到清晰层级与可读对比
- Dark：暗色主题不出现硬编码白底/黑字；表格、弹窗、抽屉、编辑器、图表和 hover 状态均可辨识
- 当前覆盖：`mixed`

### PAGE-010 `/system/dict/item`

- 模块：字典管理
- 数据成功：当前字典的字典项、值、排序、颜色和状态使用真实接口数据，字段、总数、排序、筛选和刷新结果一致
- Loading：首次加载和刷新期间显示稳定 loading，不闪现旧数据、不改变主布局尺寸
- Empty：无记录时显示明确空状态，保留可用的新增、刷新或返回动作
- Error：接口失败时显示可读错误和重试入口；旧数据不伪装成最新成功结果
- 交互成功：返回字典列表并新增、编辑、删除字典项完成后给出明确反馈，列表、详情、URL 和缓存状态同步
- 交互失败：表单校验、业务约束或网络失败时保留用户输入，关闭重复提交并显示准确原因
- 权限：无页面权限时菜单不可见且直接访问被拒绝；无动作权限时对应按钮不可见或禁用
- Desktop：父字典上下文始终可见；值、标签、颜色预览和排序列对齐，返回入口位置稳定；主工作区填满 Shell 分配的可用高度，页面头部保持稳定，表格、Tabs、树或专用画布在内容区内部滚动，底部不出现因自然高度收缩造成的无意义空白
- Narrow：窄屏下无控件重叠和横向页面溢出；复杂表格允许容器内横向滚动，关键操作仍可触达
- Light：浅色主题下背景、边框、正文、次要文字、选中态和危险操作达到清晰层级与可读对比
- Dark：暗色主题不出现硬编码白底/黑字；表格、弹窗、抽屉、编辑器、图表和 hover 状态均可辨识
- 当前覆盖：`mixed`

### PAGE-011 `/system/config`

- 模块：配置项维护
- 数据成功：配置分组、键、值、类型、敏感标记和状态使用真实接口数据，字段、总数、排序、筛选和刷新结果一致
- Loading：首次加载和刷新期间显示稳定 loading，不闪现旧数据、不改变主布局尺寸
- Empty：无记录时显示明确空状态，保留可用的新增、刷新或返回动作
- Error：接口失败时显示可读错误和重试入口；旧数据不伪装成最新成功结果
- 交互成功：新增、编辑、批量保存、删除并刷新配置缓存完成后给出明确反馈，列表、详情、URL 和缓存状态同步
- 交互失败：表单校验、业务约束或网络失败时保留用户输入，关闭重复提交并显示准确原因
- 权限：无页面权限时菜单不可见且直接访问被拒绝；无动作权限时对应按钮不可见或禁用
- Desktop：分组导航和配置表格密度适中；长值安全截断并可查看，敏感值不在表格直接展示；主工作区填满 Shell 分配的可用高度，页面头部保持稳定，表格、Tabs、树或专用画布在内容区内部滚动，底部不出现因自然高度收缩造成的无意义空白
- Narrow：窄屏下无控件重叠和横向页面溢出；复杂表格允许容器内横向滚动，关键操作仍可触达
- Light：浅色主题下背景、边框、正文、次要文字、选中态和危险操作达到清晰层级与可读对比
- Dark：暗色主题不出现硬编码白底/黑字；表格、弹窗、抽屉、编辑器、图表和 hover 状态均可辨识
- 当前覆盖：`mixed`

### PAGE-012 `/system/settings`

- 模块：系统设置
- 数据成功：基础、安全、登录、Token、上传、存储、邮件和登录方式设置使用真实接口数据，字段、总数、排序、筛选和刷新结果一致
- Loading：首次加载和刷新期间显示稳定 loading，不闪现旧数据、不改变主布局尺寸
- Empty：配置项未初始化时明确指出缺失键和初始化方式，不静默隐藏整个分区
- Error：接口失败时显示可读错误和重试入口；旧数据不伪装成最新成功结果
- 交互成功：按分区独立保存策略并执行资源管理或连接测试完成后给出明确反馈，列表、详情、URL 和缓存状态同步
- 交互失败：表单校验、业务约束或网络失败时保留用户输入，关闭重复提交并显示准确原因
- 权限：无页面权限时菜单不可见且直接访问被拒绝；无动作权限时对应按钮不可见或禁用
- Desktop：分组导航、表单标题、说明、控件和保存区对齐；设置卡片边界完整，头部和保存动作固定，长表单在卡片正文内部滚动；开关使用 Switch 而非文字按钮；主工作区填满 Shell 分配的可用高度，页面头部保持稳定，表格、Tabs、树或专用画布在内容区内部滚动，底部不出现因自然高度收缩造成的无意义空白
- Narrow：窄屏下无控件重叠和横向页面溢出；复杂表格允许容器内横向滚动，关键操作仍可触达
- Light：浅色主题下背景、边框、正文、次要文字、选中态和危险操作达到清晰层级与可读对比
- Dark：暗色主题不出现硬编码白底/黑字；表格、弹窗、抽屉、编辑器、图表和 hover 状态均可辨识
- 当前覆盖：`mixed`

### PAGE-013 `/system/file`

- 模块：文件管理
- 数据成功：文件夹、文件列表、上传进度、回收站和文件引用使用真实接口数据，字段、总数、排序、筛选和刷新结果一致
- Loading：首次加载和刷新期间显示稳定 loading，不闪现旧数据、不改变主布局尺寸
- Empty：无记录时显示明确空状态，保留可用的新增、刷新或返回动作
- Error：接口失败时显示可读错误和重试入口；旧数据不伪装成最新成功结果
- 交互成功：普通/分片上传、移动、复制、重命名、下载、恢复和清理完成后给出明确反馈，列表、详情、URL 和缓存状态同步
- 交互失败：表单校验、业务约束或网络失败时保留用户输入，关闭重复提交并显示准确原因
- 权限：无页面权限时菜单不可见且直接访问被拒绝；无动作权限时对应按钮不可见或禁用
- Desktop：目录树、文件表格和上传队列使用稳定高度；名称列优先，大小/类型/状态/操作固定，进度不会推动布局；主工作区填满 Shell 分配的可用高度，页面头部保持稳定，表格、Tabs、树或专用画布在内容区内部滚动，底部不出现因自然高度收缩造成的无意义空白
- Narrow：窄屏下无控件重叠和横向页面溢出；复杂表格允许容器内横向滚动，关键操作仍可触达
- Light：浅色主题下背景、边框、正文、次要文字、选中态和危险操作达到清晰层级与可读对比
- Dark：暗色主题不出现硬编码白底/黑字；表格、弹窗、抽屉、编辑器、图表和 hover 状态均可辨识
- 当前覆盖：`mixed`

### PAGE-014 `/system/storage`

- 模块：存储配置
- 数据成功：本地/S3 存储、默认状态、启停和连接状态使用真实接口数据，字段、总数、排序、筛选和刷新结果一致
- Loading：首次加载和刷新期间显示稳定 loading，不闪现旧数据、不改变主布局尺寸
- Empty：无记录时显示明确空状态，保留可用的新增、刷新或返回动作
- Error：接口失败时显示可读错误和重试入口；旧数据不伪装成最新成功结果
- 交互成功：新增、编辑、测试连接、切换默认和启停存储完成后给出明确反馈，列表、详情、URL 和缓存状态同步
- 交互失败：表单校验、业务约束或网络失败时保留用户输入，关闭重复提交并显示准确原因
- 权限：无页面权限时菜单不可见且直接访问被拒绝；无动作权限时对应按钮不可见或禁用
- Desktop：资源类型、默认、状态和测试结果可扫描；密钥只显示已配置状态，危险操作与普通编辑有层级区分；主工作区填满 Shell 分配的可用高度，页面头部保持稳定，表格、Tabs、树或专用画布在内容区内部滚动，底部不出现因自然高度收缩造成的无意义空白
- Narrow：窄屏下无控件重叠和横向页面溢出；复杂表格允许容器内横向滚动，关键操作仍可触达
- Light：浅色主题下背景、边框、正文、次要文字、选中态和危险操作达到清晰层级与可读对比
- Dark：暗色主题不出现硬编码白底/黑字；表格、弹窗、抽屉、编辑器、图表和 hover 状态均可辨识
- 当前覆盖：`mixed`

### PAGE-015 `/system/mail/account`

- 模块：邮件配置
- 数据成功：SMTP 账号、默认状态、启停和连接参数使用真实接口数据，字段、总数、排序、筛选和刷新结果一致
- Loading：首次加载和刷新期间显示稳定 loading，不闪现旧数据、不改变主布局尺寸
- Empty：无记录时显示明确空状态，保留可用的新增、刷新或返回动作
- Error：接口失败时显示可读错误和重试入口；旧数据不伪装成最新成功结果
- 交互成功：新增、编辑、发送测试邮件、切换默认和启停账号完成后给出明确反馈，列表、详情、URL 和缓存状态同步
- 交互失败：表单校验、业务约束或网络失败时保留用户输入，关闭重复提交并显示准确原因
- 权限：无页面权限时菜单不可见且直接访问被拒绝；无动作权限时对应按钮不可见或禁用
- Desktop：账号、主机、端口、默认、状态和操作列清楚；密码不回显，测试弹窗聚焦收件人与结果；主工作区填满 Shell 分配的可用高度，页面头部保持稳定，表格、Tabs、树或专用画布在内容区内部滚动，底部不出现因自然高度收缩造成的无意义空白
- Narrow：窄屏下无控件重叠和横向页面溢出；复杂表格允许容器内横向滚动，关键操作仍可触达
- Light：浅色主题下背景、边框、正文、次要文字、选中态和危险操作达到清晰层级与可读对比
- Dark：暗色主题不出现硬编码白底/黑字；表格、弹窗、抽屉、编辑器、图表和 hover 状态均可辨识
- 当前覆盖：`mixed`

### PAGE-016 `/system/oauth/provider`

- 模块：第三方登录
- 数据成功：OAuth Provider、端点、Client ID、Scope、映射和自动创建策略使用真实接口数据，字段、总数、排序、筛选和刷新结果一致
- Loading：首次加载和刷新期间显示稳定 loading，不闪现旧数据、不改变主布局尺寸
- Empty：无记录时显示明确空状态，保留可用的新增、刷新或返回动作
- Error：接口失败时显示可读错误和重试入口；旧数据不伪装成最新成功结果
- 交互成功：新增、编辑、测试、启停和删除 Provider完成后给出明确反馈，列表、详情、URL 和缓存状态同步
- 交互失败：表单校验、业务约束或网络失败时保留用户输入，关闭重复提交并显示准确原因
- 权限：无页面权限时菜单不可见且直接访问被拒绝；无动作权限时对应按钮不可见或禁用
- Desktop：Provider 名称与 key 优先展示，端点等长文本放详情；密钥仅显示配置状态，模板选择与自定义字段分层；主工作区填满 Shell 分配的可用高度，页面头部保持稳定，表格、Tabs、树或专用画布在内容区内部滚动，底部不出现因自然高度收缩造成的无意义空白
- Narrow：窄屏下无控件重叠和横向页面溢出；复杂表格允许容器内横向滚动，关键操作仍可触达
- Light：浅色主题下背景、边框、正文、次要文字、选中态和危险操作达到清晰层级与可读对比
- Dark：暗色主题不出现硬编码白底/黑字；表格、弹窗、抽屉、编辑器、图表和 hover 状态均可辨识
- 当前覆盖：`mixed`

### PAGE-017 `/system/sms/provider`

- 模块：短信配置
- 数据成功：SMS Provider、默认状态、Webhook、签名和密钥状态使用真实接口数据，字段、总数、排序、筛选和刷新结果一致
- Loading：首次加载和刷新期间显示稳定 loading，不闪现旧数据、不改变主布局尺寸
- Empty：无记录时显示明确空状态，保留可用的新增、刷新或返回动作
- Error：接口失败时显示可读错误和重试入口；旧数据不伪装成最新成功结果
- 交互成功：新增、编辑、测试发送、切换默认和启停 Provider完成后给出明确反馈，列表、详情、URL 和缓存状态同步
- 交互失败：表单校验、业务约束或网络失败时保留用户输入，关闭重复提交并显示准确原因
- 权限：无页面权限时菜单不可见且直接访问被拒绝；无动作权限时对应按钮不可见或禁用
- Desktop：Provider、类型、默认、状态和操作列稳定；长 URL 不撑宽表格，密钥不回显；主工作区填满 Shell 分配的可用高度，页面头部保持稳定，表格、Tabs、树或专用画布在内容区内部滚动，底部不出现因自然高度收缩造成的无意义空白
- Narrow：窄屏下无控件重叠和横向页面溢出；复杂表格允许容器内横向滚动，关键操作仍可触达
- Light：浅色主题下背景、边框、正文、次要文字、选中态和危险操作达到清晰层级与可读对比
- Dark：暗色主题不出现硬编码白底/黑字；表格、弹窗、抽屉、编辑器、图表和 hover 状态均可辨识
- 当前覆盖：`mixed`

### PAGE-018 `/system/sms/template`

- 模块：短信模板
- 数据成功：模板编码、Provider、内容、变量、状态和测试结果使用真实接口数据，字段、总数、排序、筛选和刷新结果一致
- Loading：首次加载和刷新期间显示稳定 loading，不闪现旧数据、不改变主布局尺寸
- Empty：无记录时显示明确空状态，保留可用的新增、刷新或返回动作
- Error：接口失败时显示可读错误和重试入口；旧数据不伪装成最新成功结果
- 交互成功：新增、编辑、启停、删除和测试模板渲染完成后给出明确反馈，列表、详情、URL 和缓存状态同步
- 交互失败：表单校验、业务约束或网络失败时保留用户输入，关闭重复提交并显示准确原因
- 权限：无页面权限时菜单不可见且直接访问被拒绝；无动作权限时对应按钮不可见或禁用
- Desktop：模板内容使用受控截断和详情查看；变量标签可换行，状态和操作保持固定宽度；主工作区填满 Shell 分配的可用高度，页面头部保持稳定，表格、Tabs、树或专用画布在内容区内部滚动，底部不出现因自然高度收缩造成的无意义空白
- Narrow：窄屏下无控件重叠和横向页面溢出；复杂表格允许容器内横向滚动，关键操作仍可触达
- Light：浅色主题下背景、边框、正文、次要文字、选中态和危险操作达到清晰层级与可读对比
- Dark：暗色主题不出现硬编码白底/黑字；表格、弹窗、抽屉、编辑器、图表和 hover 状态均可辨识
- 当前覆盖：`mixed`

### PAGE-019 `/system/notice`

- 模块：通知公告
- 数据成功：公告内容、范围、发布时间、有效期、置顶、优先级和阅读统计使用真实接口数据，字段、总数、排序、筛选和刷新结果一致
- Loading：首次加载和刷新期间显示稳定 loading，不闪现旧数据、不改变主布局尺寸
- Empty：无记录时显示明确空状态，保留可用的新增、刷新或返回动作
- Error：接口失败时显示可读错误和重试入口；旧数据不伪装成最新成功结果
- 交互成功：编辑、定时发布、撤回、删除并查看阅读明细完成后给出明确反馈，列表、详情、URL 和缓存状态同步
- 交互失败：表单校验、业务约束或网络失败时保留用户输入，关闭重复提交并显示准确原因
- 权限：无页面权限时菜单不可见且直接访问被拒绝；无动作权限时对应按钮不可见或禁用
- Desktop：标题列优先，发布状态/范围/时间可快速扫描；富文本编辑区、统计抽屉和详情内容宽度可读；主工作区填满 Shell 分配的可用高度，页面头部保持稳定，表格、Tabs、树或专用画布在内容区内部滚动，底部不出现因自然高度收缩造成的无意义空白
- Narrow：窄屏下无控件重叠和横向页面溢出；复杂表格允许容器内横向滚动，关键操作仍可触达
- Light：浅色主题下背景、边框、正文、次要文字、选中态和危险操作达到清晰层级与可读对比
- Dark：暗色主题不出现硬编码白底/黑字；表格、弹窗、抽屉、编辑器、图表和 hover 状态均可辨识
- 当前覆盖：`mixed`

### PAGE-020 `/system/login/log`

- 模块：登录审计
- 数据成功：登录用户、方式、IP、UA、成功状态、原因和时间使用真实接口数据，字段、总数、排序、筛选和刷新结果一致
- Loading：首次加载和刷新期间显示稳定 loading，不闪现旧数据、不改变主布局尺寸
- Empty：无记录时显示明确空状态，保留可用的新增、刷新或返回动作
- Error：接口失败时显示可读错误和重试入口；旧数据不伪装成最新成功结果
- 交互成功：筛选、查看详情、删除和按条件清理日志完成后给出明确反馈，列表、详情、URL 和缓存状态同步
- 交互失败：表单校验、业务约束或网络失败时保留用户输入，关闭重复提交并显示准确原因
- 权限：无页面权限时菜单不可见且直接访问被拒绝；无动作权限时对应按钮不可见或禁用
- Desktop：用户名、方式、结果、IP 和时间作为主列；长 UA 放详情，筛选区紧凑，分页与底边框完整；主工作区填满 Shell 分配的可用高度，页面头部保持稳定，表格、Tabs、树或专用画布在内容区内部滚动，底部不出现因自然高度收缩造成的无意义空白
- Narrow：窄屏下无控件重叠和横向页面溢出；复杂表格允许容器内横向滚动，关键操作仍可触达
- Light：浅色主题下背景、边框、正文、次要文字、选中态和危险操作达到清晰层级与可读对比
- Dark：暗色主题不出现硬编码白底/黑字；表格、弹窗、抽屉、编辑器、图表和 hover 状态均可辨识
- 当前覆盖：`mixed`

### PAGE-021 `/system/online/user`

- 模块：在线会话
- 数据成功：在线 token、用户、来源、IP、UA、最近活跃和过期时间使用真实接口数据，字段、总数、排序、筛选和刷新结果一致
- Loading：首次加载和刷新期间显示稳定 loading，不闪现旧数据、不改变主布局尺寸
- Empty：无记录时显示明确空状态，保留可用的新增、刷新或返回动作
- Error：接口失败时显示可读错误和重试入口；旧数据不伪装成最新成功结果
- 交互成功：筛选、强制单会话/用户下线并清理过期 token完成后给出明确反馈，列表、详情、URL 和缓存状态同步
- 交互失败：表单校验、业务约束或网络失败时保留用户输入，关闭重复提交并显示准确原因
- 权限：无页面权限时菜单不可见且直接访问被拒绝；无动作权限时对应按钮不可见或禁用
- Desktop：用户、来源、IP、活跃和到期时间可比较；UA 放详情，强制下线使用明确危险确认；主工作区填满 Shell 分配的可用高度，页面头部保持稳定，表格、Tabs、树或专用画布在内容区内部滚动，底部不出现因自然高度收缩造成的无意义空白
- Narrow：窄屏下无控件重叠和横向页面溢出；复杂表格允许容器内横向滚动，关键操作仍可触达
- Light：浅色主题下背景、边框、正文、次要文字、选中态和危险操作达到清晰层级与可读对比
- Dark：暗色主题不出现硬编码白底/黑字；表格、弹窗、抽屉、编辑器、图表和 hover 状态均可辨识
- 当前覆盖：`mixed`

### PAGE-022 `/system/operation/log`

- 模块：操作审计
- 数据成功：用户、模块、动作、风险、requestId、IP、状态、耗时和详情使用真实接口数据，字段、总数、排序、筛选和刷新结果一致
- Loading：首次加载和刷新期间显示稳定 loading，不闪现旧数据、不改变主布局尺寸
- Empty：无记录时显示明确空状态，保留可用的新增、刷新或返回动作
- Error：接口失败时显示可读错误和重试入口；旧数据不伪装成最新成功结果
- 交互成功：组合筛选、查看 JSON/变更摘要、复制追踪、导出和清理完成后给出明确反馈，列表、详情、URL 和缓存状态同步
- 交互失败：表单校验、业务约束或网络失败时保留用户输入，关闭重复提交并显示准确原因
- 权限：无页面权限时菜单不可见且直接访问被拒绝；无动作权限时对应按钮不可见或禁用
- Desktop：风险、动作、用户、状态和时间为主列；requestId 可复制，JSON 详情在抽屉内格式化，固定操作列不遮挡内容；主工作区填满 Shell 分配的可用高度，页面头部保持稳定，表格、Tabs、树或专用画布在内容区内部滚动，底部不出现因自然高度收缩造成的无意义空白
- Narrow：窄屏下无控件重叠和横向页面溢出；复杂表格允许容器内横向滚动，关键操作仍可触达
- Light：浅色主题下背景、边框、正文、次要文字、选中态和危险操作达到清晰层级与可读对比
- Dark：暗色主题不出现硬编码白底/黑字；表格、弹窗、抽屉、编辑器、图表和 hover 状态均可辨识
- 当前覆盖：`mixed`

### PAGE-023 `/system/ai/setup`

- 模块：AI 接入
- 数据成功：可用连接、可用模型、默认用途和分步接入状态使用真实接口数据，字段、总数、排序、筛选和刷新结果一致
- Loading：首次加载和刷新期间显示稳定 loading，不闪现旧数据、不改变主布局尺寸
- Empty：没有可用连接或默认 Chat 模型时显示接入入口，不要求用户先理解 Provider 和 Model 内部关系
- Error：接口失败时显示可读错误和重试入口；旧数据不伪装成最新成功结果
- 交互成功：选择服务商并测试连接、同步或手工添加模型、声明需要用于 Agent 的工具调用能力、批量勾选导入、设置默认用途并在一个事务中完成接入完成后给出明确反馈，列表、详情、URL 和缓存状态同步
- 交互失败：表单校验、业务约束或网络失败时保留用户输入，关闭重复提交并显示准确原因
- 权限：system.aiSetup.query 控制页面和状态读取，system.aiSetup.configure 控制连接发现和完成接入；高级页面继续使用各自权限
- Desktop：接入状态、默认模型和标准流程优先展示；向导固定操作区可触达，模型列表在弹窗中局部滚动，Chat 模型可直接声明 Agent 工具能力，高级连接参数默认折叠；主工作区填满 Shell 分配的可用高度，页面头部保持稳定，表格、Tabs、树或专用画布在内容区内部滚动，底部不出现因自然高度收缩造成的无意义空白
- Narrow：窄屏下无控件重叠和横向页面溢出；复杂表格允许容器内横向滚动，关键操作仍可触达
- Light：浅色主题下背景、边框、正文、次要文字、选中态和危险操作达到清晰层级与可读对比
- Dark：暗色主题不出现硬编码白底/黑字；表格、弹窗、抽屉、编辑器、图表和 hover 状态均可辨识
- 当前覆盖：`mixed`

### PAGE-024 `/system/ai/provider`

- 模块：AI 服务商
- 数据成功：服务商类型、连接名称、Base URL、API Key 状态、连接级请求超时、默认和启停状态；内部编码由系统维护使用真实接口数据，字段、总数、排序、筛选和刷新结果一致
- Loading：首次加载和刷新期间显示稳定 loading，不闪现旧数据、不改变主布局尺寸
- Empty：无记录时显示明确空状态，保留可用的新增、刷新或返回动作
- Error：接口失败时显示可读错误和重试入口；旧数据不伪装成最新成功结果
- 交互成功：为每个账号、环境或网关创建独立连接并配置默认请求超时；同一服务商类型允许多连接，并可测试、切默认和启停完成后给出明确反馈，列表、详情、URL 和缓存状态同步
- 交互失败：表单校验、业务约束或网络失败时保留用户输入，关闭重复提交并显示准确原因
- 权限：无页面权限时菜单不可见且直接访问被拒绝；无动作权限时对应按钮不可见或禁用
- Desktop：连接名称、服务商类型、内部编码、默认和状态可扫描；同类型多连接能明确区分，测试使用独立弹窗；主工作区填满 Shell 分配的可用高度，页面头部保持稳定，表格、Tabs、树或专用画布在内容区内部滚动，底部不出现因自然高度收缩造成的无意义空白
- Narrow：窄屏下无控件重叠和横向页面溢出；复杂表格允许容器内横向滚动，关键操作仍可触达
- Light：浅色主题下背景、边框、正文、次要文字、选中态和危险操作达到清晰层级与可读对比
- Dark：暗色主题不出现硬编码白底/黑字；表格、弹窗、抽屉、编辑器、图表和 hover 状态均可辨识
- 当前覆盖：`mixed`

### PAGE-025 `/system/ai/model`

- 模块：模型管理
- 数据成功：服务商连接实例、模型 ID、用途、默认和状态；能力、上下文、输出和价格位于高级配置使用真实接口数据，字段、总数、排序、筛选和刷新结果一致
- Loading：首次加载和刷新期间显示稳定 loading，不闪现旧数据、不改变主布局尺寸
- Empty：无记录时显示明确空状态，保留可用的新增、刷新或返回动作
- Error：接口失败时显示可读错误和重试入口；旧数据不伪装成最新成功结果
- 交互成功：选择服务商连接后同步模型并从自动展开的候选项选择；可刷新社区价格目录、查看当前值与候选值差异并逐字段显式应用，目录不会静默覆盖价格或伪造官方核验日期完成后给出明确反馈，列表、详情、URL 和缓存状态同步
- 交互失败：表单校验、业务约束或网络失败时保留用户输入，关闭重复提交并显示准确原因
- 权限：无页面权限时菜单不可见且直接访问被拒绝；无动作权限时对应按钮不可见或禁用
- Desktop：模型 ID 和用途为主信息，上下文/输出上限数字对齐；测试弹窗区分 Prompt、流输出、用量和结束原因；主工作区填满 Shell 分配的可用高度，页面头部保持稳定，表格、Tabs、树或专用画布在内容区内部滚动，底部不出现因自然高度收缩造成的无意义空白
- Narrow：窄屏下无控件重叠和横向页面溢出；复杂表格允许容器内横向滚动，关键操作仍可触达
- Light：浅色主题下背景、边框、正文、次要文字、选中态和危险操作达到清晰层级与可读对比
- Dark：暗色主题不出现硬编码白底/黑字；表格、弹窗、抽屉、编辑器、图表和 hover 状态均可辨识
- 当前覆盖：`mixed`

### PAGE-026 `/system/ai/playground`

- 模块：AI Playground
- 数据成功：已配置模型、Prompt、System Prompt、输出限制和临时流式结果，不产生正式会话使用真实接口数据，字段、总数、排序、筛选和刷新结果一致
- Loading：首次加载和刷新期间显示稳定 loading，不闪现旧数据、不改变主布局尺寸
- Empty：未配置可用模型时显示前往 Provider/模型管理的明确入口
- Error：接口失败时显示可读错误和重试入口；旧数据不伪装成最新成功结果
- 交互成功：选择任意已启用模型或默认模型，发送、停止、调整参数并查看 Markdown、用量和结束原因完成后给出明确反馈，列表、详情、URL 和缓存状态同步
- 交互失败：表单校验、业务约束或网络失败时保留用户输入，关闭重复提交并显示准确原因
- 权限：无页面权限时菜单不可见且直接访问被拒绝；无动作权限时对应按钮不可见或禁用
- Desktop：参数区与结果区比例合理；输入、发送/停止、流式正文和用量不重叠，长内容在结果区内部滚动；主工作区填满 Shell 分配的可用高度，页面头部保持稳定，表格、Tabs、树或专用画布在内容区内部滚动，底部不出现因自然高度收缩造成的无意义空白
- Narrow：窄屏下无控件重叠和横向页面溢出；复杂表格允许容器内横向滚动，关键操作仍可触达
- Light：浅色主题下背景、边框、正文、次要文字、选中态和危险操作达到清晰层级与可读对比
- Dark：暗色主题不出现硬编码白底/黑字；表格、弹窗、抽屉、编辑器、图表和 hover 状态均可辨识
- 当前覆盖：`mixed`

### PAGE-027 `/system/ai/runtime`

- 模块：AI 运行治理
- 数据成功：Chat、Agent、RAG 和 Eval 用途模型路由、Provider 健康、费用账本与调用 Trace使用真实接口数据，字段、总数、排序、筛选和刷新结果一致
- Loading：首次加载和刷新期间显示稳定 loading，不闪现旧数据、不改变主布局尺寸
- Empty：没有调用记录时用途配置仍可维护，健康和 Trace 区域分别展示明确空状态
- Error：接口失败时显示可读错误和重试入口；旧数据不伪装成最新成功结果
- 交互成功：调整主模型和候选优先级、切换统计窗口并查看 Invocation/Attempt 详情完成后给出明确反馈，列表、详情、URL 和缓存状态同步
- 交互失败：表单校验、业务约束或网络失败时保留用户输入，关闭重复提交并显示准确原因
- 权限：查询权限只允许查看运行数据；修改用途路由必须具有 system.aiRuntime.update 并写高风险操作日志
- Desktop：用途路由顺序可扫描且可编辑；健康表突出成功率和 P50/P95，Trace 表支持筛选并在抽屉展示每次模型尝试；主工作区填满 Shell 分配的可用高度，页面头部保持稳定，表格、Tabs、树或专用画布在内容区内部滚动，底部不出现因自然高度收缩造成的无意义空白
- Narrow：窄屏下无控件重叠和横向页面溢出；复杂表格允许容器内横向滚动，关键操作仍可触达
- Light：浅色主题下背景、边框、正文、次要文字、选中态和危险操作达到清晰层级与可读对比
- Dark：暗色主题不出现硬编码白底/黑字；表格、弹窗、抽屉、编辑器、图表和 hover 状态均可辨识
- 当前覆盖：`automated`

### PAGE-028 `/system/ai/knowledge`

- 模块：Knowledge/RAG
- 数据成功：按全局、部门或个人范围管理知识库、结构化分块模板、来源文件、索引状态、混合检索、RAG Run 和可追溯引用使用真实接口数据，字段、总数、排序、筛选和刷新结果一致
- Loading：首次加载和刷新期间显示稳定 loading，不闪现旧数据、不改变主布局尺寸
- Empty：没有知识库时保留创建入口；没有文档或证据时明确显示空状态或证据不足，不调用模型补写事实
- Error：接口失败时显示可读错误和重试入口；旧数据不伪装成最新成功结果
- 交互成功：创建知识库并选择自动、技术文档、段落、句子、递归或固定分块模板，直接上传知识文件或从普通文件库导入独立快照、建立或停用索引，并在指定知识库中生成带引用回答完成后给出明确反馈，列表、详情、URL 和缓存状态同步
- 交互失败：表单校验、业务约束或网络失败时保留用户输入，关闭重复提交并显示准确原因
- 权限：页面和查询、创建、更新、删除、索引、检索分别受 system.aiKnowledge 权限控制；部门和个人知识库必须在服务端执行数据范围过滤
- Desktop：首次进入自动选中第一条知识库，整行可点击且当前行保持明确选中态，右侧立即以分页表格加载来源文档；知识库与来源文档并排可扫描，左右工具栏和来源分页固定且两个表体在各自面板内独立滚动；添加来源默认直接上传为知识域文件，不进入普通文件管理、普通分组或普通文件下载链路，也可按权限从普通文件库导入独立 Knowledge 快照；导入后新文件使用 knowledge 用途域、独立物理路径和来源元数据，原文件移动或删除不影响知识文档与历史引用；普通文件选择器通过服务端关键词搜索、50 条分页和下拉滚动续页处理大数据量，跨页选择保留名称；分块配置说明目标长度、语义重叠和重新索引要求；问答区固定输入职责，回答与引用在结果区完整滚动；正文引用编号可点击并打开来源检查器，原文 Markdown、长代码和宽表格不能撑破 Drawer，检索诊断默认收起且多条引用可前后切换；主工作区填满 Shell 分配的可用高度，页面头部保持稳定，表格、Tabs、树或专用画布在内容区内部滚动，底部不出现因自然高度收缩造成的无意义空白
- Narrow：窄屏下无控件重叠和横向页面溢出；复杂表格允许容器内横向滚动，关键操作仍可触达
- Light：浅色主题下背景、边框、正文、次要文字、选中态和危险操作达到清晰层级与可读对比
- Dark：暗色主题不出现硬编码白底/黑字；表格、弹窗、抽屉、编辑器、图表和 hover 状态均可辨识
- 当前覆盖：`automated`

### PAGE-029 `/system/ai/notebook`

- 模块：AI Notebook
- 数据成功：带全局、部门或个人范围的 Notebook、知识库/文档/公开网站来源、联网搜索发现与选择导入、异步深度研究、限定来源问答、引用快照和摘要/提纲/FAQ/结构化简报 Artifact使用真实接口数据，字段、总数、排序、筛选和刷新结果一致
- Loading：首次加载和刷新期间显示稳定 loading，不闪现旧数据、不改变主布局尺寸
- Empty：没有 Notebook 时保留创建入口；没有来源时禁用问来源但允许联网研究自动寻找资料；证据不足时明确显示，不调用模型补写事实
- Error：接口失败时显示可读错误和重试入口；旧数据不伪装成最新成功结果
- 交互成功：创建或切换 Notebook、添加整个知识库/指定文档/公开网站；在底部统一输入框中切换问来源或联网研究，让 AI 自动规划检索、导入并索引可信来源、生成引用报告；查看研究步骤、引用和 Artifact完成后给出明确反馈，列表、详情、URL 和缓存状态同步
- 交互失败：表单校验、业务约束或网络失败时保留用户输入，关闭重复提交并显示准确原因
- 权限：查询、创建、更新、删除、来源、问答和 Artifact 分别受 system.aiNotebook 权限控制；Notebook 和来源同时执行服务端数据范围过滤；网站抓取拒绝内网、保留地址、危险重定向和非 HTML 响应，并对成功与失败写入脱敏操作日志
- Desktop：顶部 Notebook 身份、选择、状态和管理动作保持紧凑；来源、回答、引用与产物使用带分隔线的连接式三栏工作台，中间回答占主宽度且不嵌套卡片，来源和证据栏可独立收起；三栏内容独立滚动，统一输入框固定在回答栏底部并显示当前来源数量；输入框提供问来源和联网研究两个明确模式，研究设置仅在紧凑 Popover 中调整检索方向数与最多来源数，主流程不再弹出独立研究表单；提交研究后显示可点击的活动 Run 状态并自动打开研究检查区，请求失败恢复原输入；超过阈值仍未被 Worker 领取时显示离线或过载警告而不是无限等待动画；失败 Run 可基于原主题和设置创建新 Run，旧步骤保留，SSRF 拒绝提供代理/DNS 排查说明且不得放宽内网防护；窄屏通过来源、问答、证据分段切换而不是纵向堆叠，操作与正文不溢出；联网搜索默认只做发现，显示 Provider、结果、摘要、选择数量和降级诊断，批量导入允许部分成功且搜索摘要不能直接成为知识正文；深度研究运行中轮询并展示排队、执行、完成、失败或取消状态，步骤时间线、错误与证据可展开，完成后可直接打开带引用报告；网站来源显示网页标题、域名、抓取时间和安全的新窗口原文入口；来源删除仅在悬停、聚焦或触屏布局显现，引用行使用连续编号和可扫描元数据；回答和 Artifact 正文中的引用编号可点击并打开对应来源检查器，回答正文后不重复插入打断阅读的引用标签云；引用原文在 Drawer 中按 Markdown 正常排版，长代码和宽表格不能超出侧栏，多条引用可前后切换且关闭后保持原回答位置；浅色和深色主题均使用共享表面、边框、文字和语义色 token；主工作区填满 Shell 分配的可用高度，页面头部保持稳定，表格、Tabs、树或专用画布在内容区内部滚动，底部不出现因自然高度收缩造成的无意义空白
- Narrow：窄屏下无控件重叠和横向页面溢出；复杂表格允许容器内横向滚动，关键操作仍可触达
- Light：浅色主题下背景、边框、正文、次要文字、选中态和危险操作达到清晰层级与可读对比
- Dark：暗色主题不出现硬编码白底/黑字；表格、弹窗、抽屉、编辑器、图表和 hover 状态均可辨识
- 当前覆盖：`automated`

### PAGE-030 `/system/ai/eval`

- 模块：AI Eval / Trace
- 数据成功：带全局、部门或个人范围的 Dataset、固定输入 Case、不可覆盖的同步 Run/Result 历史、确定性断言和 Agent Run/Step/Invocation Trace使用真实接口数据，字段、总数、排序、筛选和刷新结果一致
- Loading：首次加载和刷新期间显示稳定 loading，不闪现旧数据、不改变主布局尺寸
- Empty：没有数据集时保留创建入口；没有启用 Case 时禁用运行；没有结果或 Trace 时明确显示空状态而不是伪造成功指标
- Error：接口失败时显示可读错误和重试入口；旧数据不伪装成最新成功结果
- 交互成功：创建数据集和 Case、从真实 Agent Run 固化用例、执行整组回归、查看文本/工具/延迟/Token/成本断言并追溯 Provider/Model 尝试完成后给出明确反馈，列表、详情、URL 和缓存状态同步
- 交互失败：表单校验、业务约束或网络失败时保留用户输入，关闭重复提交并显示准确原因
- 权限：查询、创建、更新、删除、执行和从 Run 保存分别受 system.aiEval 权限控制；数据集执行全局、部门和个人数据范围过滤，高风险工具在无人值守 Eval 中必须拒绝而不是自动批准
- Desktop：数据集列表和 Cases/Runs 主工作区并排；表格表头、分页和操作保持可达，Result 与 Trace 在抽屉内分层展示，长输出和断言证据可独立滚动；主工作区填满 Shell 分配的可用高度，页面头部保持稳定，表格、Tabs、树或专用画布在内容区内部滚动，底部不出现因自然高度收缩造成的无意义空白
- Narrow：窄屏下无控件重叠和横向页面溢出；复杂表格允许容器内横向滚动，关键操作仍可触达
- Light：浅色主题下背景、边框、正文、次要文字、选中态和危险操作达到清晰层级与可读对比
- Dark：暗色主题不出现硬编码白底/黑字；表格、弹窗、抽屉、编辑器、图表和 hover 状态均可辨识
- 当前覆盖：`automated`

### PAGE-031 `/system/ai/governance`

- 模块：AI 治理
- 数据成功：显式长期 Memory、Runtime Skills、MCP Server/OAuth/Tool allowlist、持久化熔断、共享配额、任务队列和计费账本使用真实接口数据，字段、总数、排序、筛选和刷新结果一致
- Loading：首次加载和刷新期间显示稳定 loading，不闪现旧数据、不改变主布局尺寸
- Empty：无 Memory、Skill、MCP、熔断、配额、任务或账本时，各表保持完整空状态和创建入口，不伪造 Provider 健康、费用或 Worker 状态
- Error：接口失败时显示可读错误和重试入口；旧数据不伪装成最新成功结果
- 交互成功：维护个人或 Agent Memory，组合 Skill 与受控 Tool，连接并同步 MCP Server，或一键部署只读系统数据 MCP 与盘点 Agent，审批 MCP Tool，查看熔断/half-open、配额、Worker Job 和结算账本完成后给出明确反馈，列表、详情、URL 和缓存状态同步
- 交互失败：表单校验、业务约束或网络失败时保留用户输入，关闭重复提交并显示准确原因
- 权限：Memory 使用 AI Chat 查询/更新权限并强制当前用户所有权；其余查询、配置、执行和审批分别受 system.aiGovernance 权限控制；MCP Secret 和 OAuth Token 永不回显
- Desktop：五个治理 Tab 保持紧凑表格密度；MCP Server、连接和 Tool 策略分层展示，内置系统数据 MCP 明确标记只读且可重复部署，长错误与 Endpoint 不撑破表格；任务、熔断和账本可按状态扫描；可执行任务超过阈值未领取或运行租约过期时显示 Worker 队列异常、数量和最老等待时长，并保持轮询恢复；主工作区填满 Shell 分配的可用高度，页面头部保持稳定，表格、Tabs、树或专用画布在内容区内部滚动，底部不出现因自然高度收缩造成的无意义空白
- Narrow：窄屏下无控件重叠和横向页面溢出；复杂表格允许容器内横向滚动，关键操作仍可触达
- Light：浅色主题下背景、边框、正文、次要文字、选中态和危险操作达到清晰层级与可读对比
- Dark：暗色主题不出现硬编码白底/黑字；表格、弹窗、抽屉、编辑器、图表和 hover 状态均可辨识
- 当前覆盖：`automated`

### PAGE-032 `/system/ai/chat`

- 模块：AI Chat
- 数据成功：会话、消息、模型、Agent、System Prompt、Run/Step、审批和使用量使用真实接口数据，字段、总数、排序、筛选和刷新结果一致
- Loading：首次加载和刷新期间显示稳定 loading，不闪现旧数据、不改变主布局尺寸
- Empty：无会话时提供直接创建或发送入口；无模型时显示配置入口而不是空白聊天框
- Error：接口失败时显示可读错误和重试入口；旧数据不伪装成最新成功结果
- 交互成功：创建/切换会话、发送/停止/重新生成、选择 Agent、处理服务端工具审批或一次性浏览器位置授权并导出完成后给出明确反馈，列表、详情、URL 和缓存状态同步
- 交互失败：表单校验、业务约束或网络失败时保留用户输入，关闭重复提交并显示准确原因
- 权限：无页面权限时菜单不可见且直接访问被拒绝；无动作权限时对应按钮不可见或禁用
- Desktop：会话栏、消息滚动区和固定输入区高度闭合；新会话默认使用通用工作助手，顶部一级对话模式可直接切换 Agent 或直接对话，并根据服务端实际可用 Tool 明确显示联网搜索可用、不可用或仅模型不联网；需要当前位置时在输入区上方显示一次性浏览器授权面板，权限触发、安全环境和 10 秒等待条件以可聚焦悬停图标解释；允许、拒绝、不支持、超时和未知错误精确区分并恢复同一 Agent 流程，内部续跑指令只显示为友好定位状态，不会原样暴露或误弹管理员运行检查器；输入区只保留消息与发送/停止动作，模型输出上限和超时由服务端按模型配置及安全策略兜底；首字返回前在 Assistant 行持续显示正在思考或处理任务、动态状态和已等待时长，首字出现后无缝切换 Streamdown；用户/AI 消息左右语义明确，回答底部用语义信息色突出首次响应并紧凑展示首字、可观测思考和总耗时；普通模型会话不显示运行检查器，Agent 会话可按需打开，服务端审批或运行失败时自动展开，步骤详情可完整滚动查看输入、输出、Usage、耗时和错误；主工作区填满 Shell 分配的可用高度，页面头部保持稳定，表格、Tabs、树或专用画布在内容区内部滚动，底部不出现因自然高度收缩造成的无意义空白
- Narrow：窄屏下无控件重叠和横向页面溢出；复杂表格允许容器内横向滚动，关键操作仍可触达
- Light：浅色主题下背景、边框、正文、次要文字、选中态和危险操作达到清晰层级与可读对比
- Dark：暗色主题不出现硬编码白底/黑字；表格、弹窗、抽屉、编辑器、图表和 hover 状态均可辨识
- 当前覆盖：`mixed`

### PAGE-033 `/system/ai/agent`

- 模块：AI Agent
- 数据成功：可复用 Agent 定义、受控 Tool、模型、System Prompt、审批证据、调试 Run/Step/Approval 和可信 Workflow Run使用真实接口数据，字段、总数、排序、筛选和刷新结果一致
- Loading：首次加载和刷新期间显示稳定 loading，不闪现旧数据、不改变主布局尺寸
- Empty：无 Agent 时提供创建入口和必要字段，不只展示不可操作的通用助手文案
- Error：接口失败时显示可读错误和重试入口；旧数据不伪装成最新成功结果
- 交互成功：创建/编辑 Agent 和 Tool、运行 Agent 调试或 AI 环境预检，并审批或拒绝高风险工具完成后给出明确反馈，列表、详情、URL 和缓存状态同步
- 交互失败：表单校验、业务约束或网络失败时保留用户输入，关闭重复提交并显示准确原因
- 权限：无页面权限时菜单不可见且直接访问被拒绝；无动作权限时对应按钮不可见或禁用
- Desktop：Agent、Tools、Runs、Workflows 分区清楚；各 Tab 在固定工作区内闭合高度，表头和底部分页保持可达，长列表只在表体内部滚动；预检报告和持久步骤可追踪，审批展示计划哈希、影响文件、验证输出和有效期；主工作区填满 Shell 分配的可用高度，页面头部保持稳定，表格、Tabs、树或专用画布在内容区内部滚动，底部不出现因自然高度收缩造成的无意义空白
- Narrow：窄屏下无控件重叠和横向页面溢出；复杂表格允许容器内横向滚动，关键操作仍可触达
- Light：浅色主题下背景、边框、正文、次要文字、选中态和危险操作达到清晰层级与可读对比
- Dark：暗色主题不出现硬编码白底/黑字；表格、弹窗、抽屉、编辑器、图表和 hover 状态均可辨识
- 当前覆盖：`mixed`

### PAGE-034 `/system/ai/workflow`

- 模块：AI Workflow
- 数据成功：Workflow 列表、草稿、版本、全屏无限画布、节点配置、Builder 预检和发布状态使用真实接口数据，字段、总数、排序、筛选和刷新结果一致
- Loading：首次加载和刷新期间显示稳定 loading，不闪现旧数据、不改变主布局尺寸
- Empty：没有 Workflow 时显示新建入口；没有选中节点时右侧显示节点配置空状态；没有已发布版本时禁用测试运行
- Error：接口失败时显示可读错误和重试入口；旧数据不伪装成最新成功结果
- 交互成功：从列表打开或新建，拖放节点，切换平移/框选，多选、复制粘贴、复制一份、删除、撤销重做、连接并配置节点，然后保存草稿、发布版本、测试运行并返回列表完成后给出明确反馈，列表、详情、URL 和缓存状态同步
- 交互失败：表单校验、业务约束或网络失败时保留用户输入，关闭重复提交并显示准确原因
- 权限：使用 system.aiAgent.query 访问 Workflow 控制面，保存、发布和执行继续分别由后端 Agent Workflow abilities 与审计规则保护
- Desktop：列表与编辑器分离；编辑器使用整块可用高度，节点面板、无限画布、画布操作条、右侧配置面板和底部运行抽屉各自承担单一职责；操作条支持横向滚动且不遮挡节点，名称、编码、草稿状态、版本和发布动作在稳定顶栏内，画布不被运行记录挤压；主工作区填满 Shell 分配的可用高度，页面头部保持稳定，表格、Tabs、树或专用画布在内容区内部滚动，底部不出现因自然高度收缩造成的无意义空白
- Narrow：窄屏下无控件重叠和横向页面溢出；复杂表格允许容器内横向滚动，关键操作仍可触达
- Light：浅色主题下背景、边框、正文、次要文字、选中态和危险操作达到清晰层级与可读对比
- Dark：暗色主题不出现硬编码白底/黑字；表格、弹窗、抽屉、编辑器、图表和 hover 状态均可辨识
- 当前覆盖：`mixed`

### PAGE-035 `/system/ai/web-search`

- 模块：联网搜索
- 数据成功：Tavily、Brave、SearXNG 搜索连接、密钥状态、超时、结果上限、优先级和测试结果使用真实接口数据，字段、总数、排序、筛选和刷新结果一致
- Loading：首次加载和刷新期间显示稳定 loading，不闪现旧数据、不改变主布局尺寸
- Empty：没有自定义连接时仍显示三个停用的内置模板；没有启用连接时 Agent 不暴露联网搜索 Tool
- Error：接口失败时显示可读错误和重试入口；旧数据不伪装成最新成功结果
- 交互成功：新增或配置搜索连接、启停、调整优先级并在独立弹窗执行真实搜索测试完成后给出明确反馈，列表、详情、URL 和缓存状态同步
- 交互失败：表单校验、业务约束或网络失败时保留用户输入，关闭重复提交并显示准确原因
- 权限：查询、创建、编辑、删除、启停和测试分别由 system.aiWebSearch 权限控制
- Desktop：首选连接、服务类型、端点、密钥状态、启停和优先级可扫描；测试弹窗分别展示尝试链和来源结果；主工作区填满 Shell 分配的可用高度，页面头部保持稳定，表格、Tabs、树或专用画布在内容区内部滚动，底部不出现因自然高度收缩造成的无意义空白
- Narrow：窄屏下无控件重叠和横向页面溢出；复杂表格允许容器内横向滚动，关键操作仍可触达
- Light：浅色主题下背景、边框、正文、次要文字、选中态和危险操作达到清晰层级与可读对比
- Dark：暗色主题不出现硬编码白底/黑字；表格、弹窗、抽屉、编辑器、图表和 hover 状态均可辨识
- 当前覆盖：`mixed`

### PAGE-036 `/system/module/generator`

- 模块：模块生成器
- 数据成功：共享模块契约、字段、草稿、发布差异、隔离预检、发布记录和源码回滚状态使用真实接口数据，字段、总数、排序、筛选和刷新结果一致
- Loading：首次加载和刷新期间显示稳定 loading，不闪现旧数据、不改变主布局尺寸
- Empty：没有草稿时展示创建入口；缺少开发环境能力时说明不可发布原因
- Error：接口失败时显示可读错误和重试入口；旧数据不伪装成最新成功结果
- 交互成功：生成草稿、检查逐文件差异、隔离预检、发布到真实项目并按快照回滚源码完成后给出明确反馈，列表、详情、URL 和缓存状态同步
- 交互失败：表单校验、业务约束或网络失败时保留用户输入，关闭重复提交并显示准确原因
- 权限：仅开发/测试环境和有权限用户可用，生产环境发布动作必须拒绝
- Desktop：定义表单、文件预览、草稿/已发布状态和发布风险分区明确；发布前后源码并排可滚动且不撑破页面；主工作区填满 Shell 分配的可用高度，页面头部保持稳定，表格、Tabs、树或专用画布在内容区内部滚动，底部不出现因自然高度收缩造成的无意义空白
- Narrow：窄屏下无控件重叠和横向页面溢出；复杂表格允许容器内横向滚动，关键操作仍可触达
- Light：浅色主题下背景、边框、正文、次要文字、选中态和危险操作达到清晰层级与可读对比
- Dark：暗色主题不出现硬编码白底/黑字；表格、弹窗、抽屉、编辑器、图表和 hover 状态均可辨识
- 当前覆盖：`mixed`

## 4. 视觉执行视口

- Desktop：1440 x 900，覆盖展开/收起侧栏以及侧边、混合、分栏、顶部菜单模式。
- Narrow desktop：1024 x 768，重点检查表格横向滚动、搜索区换行和弹层可用性。
- Mobile acceptance：390 x 844，只要求关键工作流可达、无页面级溢出和遮挡；高密度后台表格允许容器内滚动。
- 每个视口至少执行浅色和暗色；主题切换后弹窗、抽屉、Dropdown、Tooltip 等 Portal 内容也必须正确。

## 5. 视觉结果记录

每个页面记录版本、路由、布局模式、主题、视口、结果和截图路径。数据正确但视觉失败仍记为 `Fail`，不能以接口成功替代页面验收。
