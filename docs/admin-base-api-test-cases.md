# Admin Base API 测试用例矩阵

> 该文件由 `pnpm test:docs` 根据 `tests/coverage/api-test-cases.ts` 生成。请修改机器可读清单，不要直接维护本文件。

## 1. 范围与判定

- 当前登记 API 操作：**388** 个。
- 覆盖状态：`automated` 328，`planned` 43，`environment` 17。
- 成功不仅指 HTTP 2xx，还必须同时满足响应契约、数据事实、权限范围、副作用和审计要求。
- 失败不仅指返回错误，还必须验证无越权、无敏感信息泄漏、无部分写入、可追踪且可以恢复或重试。
- `automated` 表示已有自动化覆盖该模块主路径，不等于该操作的每个分支均已自动化；逐接口自动化仍按风险递增补齐。
- `environment` 表示需要真实 SMTP、S3、OAuth、SMS 或 AI Provider；`manual` 表示当前主要依赖手工验收；`planned` 表示用例已登记但自动化尚未落地。

## SaaS 控制面

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-001 | `DELETE /api/saas/tenant-members/{tenantId}/{userId}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-002 | `DELETE /api/saas/workspace-members/{workspaceId}/{userId}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-003 | `GET /api/saas/context` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-004 | `GET /api/saas/tenants` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-005 | `GET /api/saas/workspaces` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-006 | `GET /api/saas/tenant-members` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-007 | `GET /api/saas/workspace-members` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-008 | `GET /api/saas/invitations` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-009 | `GET /api/saas/modules` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-010 | `GET /api/saas/modules/effective` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-011 | `GET /api/saas/entitlements` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-012 | `POST /api/saas/invitations` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-013 | `POST /api/saas/invitations/{id}/revoke` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-014 | `POST /api/saas/invitations/accept` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-015 | `POST /api/saas/modules` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-016 | `POST /api/saas/entitlements` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-017 | `POST /api/saas/tenants` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-018 | `POST /api/saas/workspaces` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-019 | `PUT /api/saas/tenant-members/{tenantId}/{userId}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-020 | `PUT /api/saas/workspace-members/{workspaceId}/{userId}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-021 | `PUT /api/saas/modules/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-022 | `PUT /api/saas/entitlements/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-023 | `PUT /api/saas/tenants/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-024 | `PUT /api/saas/workspaces/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## ai / agent

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-025 | `DELETE /api/system/ai/agent/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-026 | `GET /api/system/ai/agent` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-027 | `GET /api/system/ai/agent/options` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-028 | `GET /api/system/ai/agent/runs` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-029 | `GET /api/system/ai/agent/runs/{id}/steps` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-030 | `GET /api/system/ai/agent/runs/{id}/trace` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-031 | `POST /api/system/ai/agent` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-032 | `PUT /api/system/ai/agent/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## ai / eval

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-033 | `DELETE /api/system/ai/eval/cases/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-034 | `DELETE /api/system/ai/eval/datasets/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-035 | `GET /api/system/ai/eval/datasets` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-036 | `GET /api/system/ai/eval/datasets/{id}` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-037 | `GET /api/system/ai/eval/datasets/{id}/cases` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-038 | `GET /api/system/ai/eval/options` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-039 | `GET /api/system/ai/eval/results/{id}` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-040 | `GET /api/system/ai/eval/runs` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-041 | `GET /api/system/ai/eval/runs/{id}` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-042 | `GET /api/system/ai/eval/runs/{id}/results` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-043 | `POST /api/system/ai/eval/cases/from-run/{runId}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-044 | `POST /api/system/ai/eval/datasets` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-045 | `POST /api/system/ai/eval/datasets/{id}/cases` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-046 | `POST /api/system/ai/eval/datasets/{id}/runs` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-047 | `PUT /api/system/ai/eval/cases/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-048 | `PUT /api/system/ai/eval/datasets/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## ai / knowledge

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-049 | `DELETE /api/system/ai/knowledge/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-050 | `DELETE /api/system/ai/knowledge/documents/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-051 | `GET /api/system/ai/knowledge` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-052 | `GET /api/system/ai/knowledge/{id}/documents` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-053 | `GET /api/system/ai/knowledge/{id}/source-files` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-054 | `GET /api/system/ai/knowledge/runs/{id}` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-055 | `POST /api/system/ai/knowledge` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-056 | `POST /api/system/ai/knowledge/{id}/documents` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-057 | `POST /api/system/ai/knowledge/{id}/documents/upload` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-058 | `POST /api/system/ai/knowledge/documents/{id}/index` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-059 | `POST /api/system/ai/knowledge/search` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-060 | `POST /api/system/ai/knowledge/ask` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-061 | `PUT /api/system/ai/knowledge/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-062 | `PUT /api/system/ai/knowledge/documents/{id}/status` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## ai / notebook

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-063 | `DELETE /api/system/ai/notebook/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-064 | `DELETE /api/system/ai/notebook/{id}/members/{userId}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-065 | `DELETE /api/system/ai/notebook/{id}/sources/{sourceId}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-066 | `DELETE /api/system/ai/notebook/{id}/research/{runId}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-067 | `DELETE /api/system/ai/notebook/artifacts/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-068 | `GET /api/system/ai/notebook` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-069 | `GET /api/system/ai/notebook/options` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-070 | `GET /api/system/ai/notebook/source-options` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-071 | `GET /api/system/ai/notebook/{id}` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-072 | `GET /api/system/ai/notebook/{id}/sources` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-073 | `GET /api/system/ai/notebook/{id}/sources/search/candidates` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-074 | `GET /api/system/ai/notebook/{id}/artifacts` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-075 | `GET /api/system/ai/notebook/{id}/members` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-076 | `GET /api/system/ai/notebook/{id}/research` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-077 | `GET /api/system/ai/notebook/{id}/research/{runId}` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-078 | `POST /api/system/ai/notebook` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-079 | `POST /api/system/ai/notebook/{id}/sources` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-080 | `POST /api/system/ai/notebook/{id}/sources/website` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-081 | `POST /api/system/ai/notebook/{id}/sources/search` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-082 | `POST /api/system/ai/notebook/{id}/sources/search/import` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-083 | `POST /api/system/ai/notebook/{id}/sources/search/candidates/{candidateId}/reject` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-084 | `POST /api/system/ai/notebook/{id}/research` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-085 | `POST /api/system/ai/notebook/{id}/ask` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-086 | `POST /api/system/ai/notebook/{id}/artifacts` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-087 | `POST /api/system/ai/notebook/{id}/artifacts/async` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-088 | `POST /api/system/ai/notebook/artifacts/{id}/regenerate` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-089 | `PUT /api/system/ai/notebook/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-090 | `PUT /api/system/ai/notebook/{id}/members` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## ai / governance

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-091 | `DELETE /api/system/ai/governance/memories/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-092 | `DELETE /api/system/ai/governance/skills/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-093 | `DELETE /api/system/ai/governance/mcp/servers/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-094 | `DELETE /api/system/ai/governance/mcp/connections/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-095 | `GET /api/system/ai/governance/options` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-096 | `GET /api/system/ai/governance/memories` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-097 | `GET /api/system/ai/governance/memory-candidates` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-098 | `GET /api/system/ai/governance/skills` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-099 | `GET /api/system/ai/governance/skills/{id}/versions` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-100 | `GET /api/system/ai/governance/mcp/servers` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-101 | `GET /api/system/ai/governance/mcp/connections` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-102 | `GET /api/system/ai/governance/mcp/tools` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-103 | `GET /api/system/ai/governance/circuits` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-104 | `GET /api/system/ai/governance/quotas` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-105 | `GET /api/system/ai/governance/billing` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-106 | `GET /api/system/ai/governance/jobs` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-107 | `POST /api/system/ai/governance/memories` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-108 | `POST /api/system/ai/governance/skills` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-109 | `POST /api/system/ai/governance/skills/{id}/versions` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-110 | `POST /api/system/ai/governance/skills/{id}/versions/{version}/publish` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-111 | `POST /api/system/ai/governance/skills/{id}/versions/{version}/rollback` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-112 | `POST /api/system/ai/governance/mcp/servers` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-113 | `POST /api/system/ai/governance/mcp/servers/{id}/connect` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-114 | `POST /api/system/ai/governance/mcp/servers/{id}/sync` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-115 | `POST /api/system/ai/governance/mcp/internal/oauth/token` | 1. 启用的内置 Server 使用正确 Client Credentials 换取短期 Access Token 和可轮换 Refresh Token<br>2. Refresh Token grant 校验签名、类型和有效期后签发新 Token，响应符合 OAuth token 结构 | 1. Client ID、Client Secret、grant_type 或 Refresh Token 无效时返回 4xx，不签发任何 Token<br>2. Server 被停用、删除或轮换 Secret 后，旧凭据和旧 Token 立即失效 | 1. Token 只绑定当前内置 MCP Server、Token 类型和过期时间，scope 固定为 system.read<br>2. 响应不包含保存的加密 Secret、Server 数据库字段或认证用户资料 | 1. 接口不使用后台登录 Token，而使用独立 Client Credentials；Secret 比较采用固定时间摘要比较<br>2. Client Secret、Access Token 和 Refresh Token 不写入 operation log 或普通请求日志 | 1. 换证不修改业务数据，重新部署内置 MCP 时由治理动作负责轮换 Secret 和撤销旧连接 | `automated` |
| API-116 | `POST /api/system/ai/governance/mcp/internal` | 1. 有效 MCP Access Token 和调用用户可完成 initialize、initialized、ping、tools/list 与 tools/call<br>2. 五个 Tool 可读取系统概览、页面、菜单树、角色权限和脱敏人员组织数据 | 1. Token 缺失、签名错误、过期或 Server 停用时返回 401；调用用户无效或缺少查询权限时返回 403<br>2. 未知 Tool、非法参数和未知 JSON-RPC method 返回受控错误，不能转化为任意 SQL 或写操作 | 1. 页面、菜单和权限来自 sys_rule；人员结果支持分页并只包含用户名、昵称、部门、角色和状态<br>2. 响应不含 passwordHash、Token、手机号、邮箱或其他认证凭据 | 1. MCP Client 与当前调用用户同时校验，用户必须启用并拥有 system.aiGovernance.query<br>2. 只暴露固定只读 Tool 注册表，不接受 SQL、表名、URL、脚本或任意 handler | 1. 所有 tools/call 都是数据库只读查询，不修改菜单、权限、角色、用户或部门数据 | `automated` |
| API-117 | `POST /api/system/ai/governance/mcp/internal/provision` | 1. 一键创建或更新 admin-base-system Server，完成连接、Tool 同步、allowlist 和系统 Agent 绑定<br>2. 重复部署轮换 Client Secret、撤销旧连接并修复五个 Tool 策略，不创建重复 Server 或 Agent | 1. 未登录返回 401，缺少 update、execute 或 approve 任一权限返回 403且不改变现有配置<br>2. 连接、同步或 Agent 保存失败时返回明确错误，不回显 Client Secret 或 OAuth Token | 1. 五个 MCP Tool 均为 enabled、allowlisted、low risk、无需审批，并映射到同一系统 Agent<br>2. Server、活动 Connection、镜像 Tool、Agent 及 Agent-Tool 关联与响应 ID 一致 | 1. 部署要求 system.aiGovernance.update、execute 和 approve 三项权限<br>2. 生成的 Client Secret 只加密保存，响应、操作日志和 UI 都不得回显 | 1. 写入 critical 级 operation log，details 只记录固定 code 和 read_only 模式<br>2. 旧活动连接被撤销，旧 Access/Refresh Token 被清空；系统 Agent 作为内置资源禁止误删 | `automated` |
| API-118 | `POST /api/system/ai/governance/memory-candidates/{id}/decision` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-119 | `POST /api/system/ai/governance/circuits/reset` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-120 | `POST /api/system/ai/governance/quotas` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-121 | `POST /api/system/ai/governance/billing/adjustments` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-122 | `POST /api/system/ai/governance/jobs/eval` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-123 | `POST /api/system/ai/governance/jobs/{id}/retry` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-124 | `POST /api/system/ai/governance/jobs/{id}/cancel` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-125 | `PUT /api/system/ai/governance/memories/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-126 | `PUT /api/system/ai/governance/skills/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-127 | `PUT /api/system/ai/governance/mcp/servers/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-128 | `PUT /api/system/ai/governance/mcp/tools/{id}/policy` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-129 | `PUT /api/system/ai/governance/circuits` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-130 | `PUT /api/system/ai/governance/quotas/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-131 | `PUT /api/system/ai/governance/billing/{id}/settlement` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## ai / chat

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-132 | `DELETE /api/system/ai/chat/sessions/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-133 | `GET /api/system/ai/chat/options` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-134 | `GET /api/system/ai/chat/runtime-config` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-135 | `GET /api/system/ai/chat/sessions` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-136 | `GET /api/system/ai/chat/sessions/{id}/approvals` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-137 | `GET /api/system/ai/chat/sessions/{id}/export` | 1. 合法请求返回文件或导出内容，文件名、Content-Type、长度和内容与目标记录/筛选一致<br>2. 文件读取支持合法 Range 时返回 206；下载包含 nosniff，危险文件不以内联可执行方式响应 | 1. 文件不存在或越权时返回 404/403；路径穿越被拒绝；非法 Range 返回 416 和正确 Content-Range<br>2. 存储读取失败时不返回截断但标记成功的文件，不泄漏本地绝对路径、Bucket 密钥或内部堆栈 | 1. 响应字节、文件大小、MIME、文件名和 Range 区间与存储元数据及源文件一致<br>2. 导出内容严格使用当前筛选和数据权限，危险文件响应头符合下载策略 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-138 | `GET /api/system/ai/chat/sessions/{id}/messages` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-139 | `GET /api/system/ai/chat/sessions/{id}/run/latest` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-140 | `GET /api/system/ai/chat/sessions/{id}/runs/{runId}/events` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-141 | `POST /api/system/ai/chat/sessions/{sessionId}/client-actions/{id}/result` | 1. granted 结果在浏览器和服务端分别粗化后写入 Approval/Step，Run 进入 waiting_continuation 并恢复同一 Agent 流程<br>2. denied、unavailable 或 error 作为合法 Client Tool 结果完成当前 Step，Agent 随后询问城市而不是把会话标记失败 | 1. 未登录返回 401，缺少 system.aiChat.chat 返回 403；跨用户、跨会话、非 browser-location Tool 返回 409<br>2. 过期或已处理结果返回 409，重复提交不能创建第二条消息、执行第二次续跑或覆盖原结果 | 1. 经纬度只保留两位小数，accuracy 为受限安全整数；Approval、Step 和系统上下文不含客户端提交的精确坐标<br>2. Approval 为 executed、Step 为 completed、父 Run 为 waiting_continuation；过期请求将 Step/Run 收口为 denied/stopped | 1. 接口只要求当前会话用户的 system.aiChat.chat，不使用管理员 system.aiAgent.approve 权限，也不能执行任意服务端 Tool<br>2. 请求体使用判别联合严格校验；服务端只构造受控输出和系统消息，不接受客户端自定义 output 或日志字段 | 1. 成功或降级结果刷新会话消息统计并允许一次续跑；过期结果停止等待中的 Run，避免永久挂起<br>2. 操作日志只记录 browser_location、结果状态和是否授权，不记录经纬度、accuracy 或浏览器原始错误对象 | `automated` |
| API-142 | `POST /api/system/ai/chat/sessions` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-143 | `POST /api/system/ai/chat/sessions/{id}/messages/{messageId}/regenerate` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-144 | `POST /api/system/ai/chat/sessions/{id}/messages/stream` | 1. 合法输入产生可持续消费的流，meta/delta/tool/finish 或错误事件顺序符合协议<br>2. 正文增量、usage、finishReason 和最终持久化状态一致，客户端结束后不残留 loading | 1. 未登录返回 401；缺少对应 ability 返回 403，且不启动模型或外部请求<br>2. 参数非法、Provider 超时/断流、主动取消或工具失败时结束流并持久化准确状态，不重复消息、Step 或副作用 | 1. 每个事件可解析，delta 拼接结果等于完成文本，usage 和 finishReason 写入最终记录<br>2. 失败、取消和 length 截断状态可区分，Run/Step/消息不会长期停留 running | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `environment` |
| API-145 | `PUT /api/system/ai/chat/sessions/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## ai / model

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-146 | `DELETE /api/system/ai/model/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-147 | `GET /api/system/ai/model` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-148 | `GET /api/system/ai/model/{id}/pricing/preview` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-149 | `POST /api/system/ai/model` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-150 | `POST /api/system/ai/model/test` | 1. 合法临时参数或已保存资源完成真实连接/请求测试，并返回可读的耗时与结果<br>2. 测试不覆盖已保存密钥，不改变默认资源或业务数据，流式测试能够正常结束 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `environment` |
| API-151 | `POST /api/system/ai/model/test/stream` | 1. 合法输入产生可持续消费的流，meta/delta/tool/finish 或错误事件顺序符合协议<br>2. 正文增量、usage、finishReason 和最终持久化状态一致，客户端结束后不残留 loading | 1. 未登录返回 401；缺少对应 ability 返回 403，且不启动模型或外部请求<br>2. 参数非法、Provider 超时/断流、主动取消或工具失败时结束流并持久化准确状态，不重复消息、Step 或副作用 | 1. 每个事件可解析，delta 拼接结果等于完成文本，usage 和 finishReason 写入最终记录<br>2. 失败、取消和 length 截断状态可区分，Run/Step/消息不会长期停留 running | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `environment` |
| API-152 | `PUT /api/system/ai/model/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-153 | `PUT /api/system/ai/model/default/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-154 | `PUT /api/system/ai/model/status/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-155 | `PUT /api/system/ai/model/{id}/pricing/apply` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## ai / provider

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-156 | `DELETE /api/system/ai/provider/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-157 | `GET /api/system/ai/provider/{id}/models` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-158 | `GET /api/system/ai/provider/{id}/test-models` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `environment` |
| API-159 | `GET /api/system/ai/provider` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-160 | `POST /api/system/ai/provider` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-161 | `POST /api/system/ai/provider/batch-delete` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-162 | `POST /api/system/ai/provider/test` | 1. 合法临时参数或已保存资源完成真实连接/请求测试，并返回可读的耗时与结果<br>2. 测试不覆盖已保存密钥，不改变默认资源或业务数据，流式测试能够正常结束 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `environment` |
| API-163 | `POST /api/system/ai/provider/test/stream` | 1. 合法输入产生可持续消费的流，meta/delta/tool/finish 或错误事件顺序符合协议<br>2. 正文增量、usage、finishReason 和最终持久化状态一致，客户端结束后不残留 loading | 1. 未登录返回 401；缺少对应 ability 返回 403，且不启动模型或外部请求<br>2. 参数非法、Provider 超时/断流、主动取消或工具失败时结束流并持久化准确状态，不重复消息、Step 或副作用 | 1. 每个事件可解析，delta 拼接结果等于完成文本，usage 和 finishReason 写入最终记录<br>2. 失败、取消和 length 截断状态可区分，Run/Step/消息不会长期停留 running | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `environment` |
| API-164 | `PUT /api/system/ai/provider/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-165 | `PUT /api/system/ai/provider/default/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-166 | `PUT /api/system/ai/provider/status/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## ai / web-search

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-167 | `DELETE /api/system/ai/web-search/provider/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-168 | `GET /api/system/ai/web-search/provider` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-169 | `POST /api/system/ai/web-search/provider` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-170 | `POST /api/system/ai/web-search/provider/batch-delete` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-171 | `POST /api/system/ai/web-search/provider/test` | 1. 合法临时参数或已保存资源完成真实连接/请求测试，并返回可读的耗时与结果<br>2. 测试不覆盖已保存密钥，不改变默认资源或业务数据，流式测试能够正常结束 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `environment` |
| API-172 | `PUT /api/system/ai/web-search/provider/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-173 | `PUT /api/system/ai/web-search/provider/status/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## ai / tool

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-174 | `DELETE /api/system/ai/tool/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-175 | `GET /api/system/ai/tool` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-176 | `POST /api/system/ai/tool` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-177 | `PUT /api/system/ai/tool/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## config / group

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-178 | `DELETE /api/system/config/group/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-179 | `GET /api/system/config/group` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-180 | `POST /api/system/config/group` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-181 | `POST /api/system/config/group/batch-delete` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-182 | `PUT /api/system/config/group/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## config / items

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-183 | `DELETE /api/system/config/items/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-184 | `GET /api/system/config/items` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-185 | `POST /api/system/config/items` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-186 | `POST /api/system/config/items/batch-delete` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-187 | `POST /api/system/config/items/refreshCache` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-188 | `PUT /api/system/config/items/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-189 | `PUT /api/system/config/items/save` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## dept

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-190 | `DELETE /api/system/dept/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-191 | `GET /api/system/dept` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-192 | `POST /api/system/dept` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-193 | `PUT /api/system/dept/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## dict / item

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-194 | `DELETE /api/system/dict/item/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-195 | `GET /api/system/dict/item` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-196 | `POST /api/system/dict/item` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-197 | `POST /api/system/dict/item/batch-delete` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-198 | `PUT /api/system/dict/item/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## dict / list

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-199 | `DELETE /api/system/dict/list/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-200 | `GET /api/system/dict/list` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-201 | `GET /api/system/dict/list/all` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-202 | `POST /api/system/dict/list` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-203 | `POST /api/system/dict/list/batch-delete` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-204 | `PUT /api/system/dict/list/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## file / chunk

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-205 | `DELETE /api/system/file/chunk/{uploadId}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-206 | `DELETE /api/system/file/chunk/clean-expired` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-207 | `POST /api/system/file/chunk/complete` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-208 | `POST /api/system/file/chunk/init` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-209 | `POST /api/system/file/chunk/part` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## file / group

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-210 | `DELETE /api/system/file/group/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-211 | `GET /api/system/file/group/tree` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-212 | `POST /api/system/file/group` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-213 | `PUT /api/system/file/group/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## file / list

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-214 | `DELETE /api/system/file/list/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-215 | `DELETE /api/system/file/list/clean-trash` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-216 | `DELETE /api/system/file/list/force/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-217 | `GET /api/system/file/list` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-218 | `GET /api/system/file/list/download/{id}` | 1. 合法请求返回文件或导出内容，文件名、Content-Type、长度和内容与目标记录/筛选一致<br>2. 文件读取支持合法 Range 时返回 206；下载包含 nosniff，危险文件不以内联可执行方式响应 | 1. 文件不存在或越权时返回 404/403；路径穿越被拒绝；非法 Range 返回 416 和正确 Content-Range<br>2. 存储读取失败时不返回截断但标记成功的文件，不泄漏本地绝对路径、Bucket 密钥或内部堆栈 | 1. 响应字节、文件大小、MIME、文件名和 Range 区间与存储元数据及源文件一致<br>2. 导出内容严格使用当前筛选和数据权限，危险文件响应头符合下载策略 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `environment` |
| API-219 | `GET /api/system/file/list/trash` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-220 | `POST /api/system/file/list/batch-delete` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-221 | `POST /api/system/file/list/batch-force` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-222 | `POST /api/system/file/list/batch-restore` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-223 | `POST /api/system/file/list/copy` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-224 | `POST /api/system/file/list/upload` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-225 | `PUT /api/system/file/list/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-226 | `PUT /api/system/file/list/move` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-227 | `PUT /api/system/file/list/rename/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-228 | `PUT /api/system/file/list/restore/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## file / reference

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-229 | `DELETE /api/system/file/reference` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-230 | `POST /api/system/file/reference` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## 认证安全

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-231 | `DELETE /api/system/login/log/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-232 | `DELETE /api/system/login/log/clean` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-233 | `DELETE /api/system/oauth/provider/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-234 | `DELETE /api/system/profile/oauth/{provider}/unbind` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-235 | `GET /api/system/ai/governance/mcp/oauth/callback` | 1. 合法 Provider 和 state/code 完成 HTTP 302 跳转，Location 仅指向允许的本站或 Provider 地址<br>2. callback 按登录/绑定模式创建正确 token 或绑定关系，并记录 OAuth 登录方式和结果 | 1. Provider 停用/缺配置、state 缺失/过期/不匹配、code 缺失或上游失败时拒绝，不创建 token 或绑定<br>2. 失败写登录/操作日志但不暴露 clientSecret、上游 token、内部堆栈或账号枚举信息 | 1. state 单次消费，OAuth account/user/token 关联与回调身份一致<br>2. 失败前后账号绑定数、用户权限和现有 token 保持不变 | 1. 接口允许匿名访问，但必须执行限流、状态校验或防枚举规则<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `environment` |
| API-236 | `GET /api/system/login/captcha` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 缺少必填参数、参数过期或凭据错误时返回统一错误结构，不暴露账号、密钥或内部堆栈<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口允许匿名访问，但必须执行限流、状态校验或防枚举规则<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-237 | `GET /api/system/login/log` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-238 | `GET /api/system/login/options` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 缺少必填参数、参数过期或凭据错误时返回统一错误结构，不暴露账号、密钥或内部堆栈<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口允许匿名访问，但必须执行限流、状态校验或防枚举规则<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-239 | `GET /api/system/oauth/{provider}/callback` | 1. 合法 Provider 和 state/code 完成 HTTP 302 跳转，Location 仅指向允许的本站或 Provider 地址<br>2. callback 按登录/绑定模式创建正确 token 或绑定关系，并记录 OAuth 登录方式和结果 | 1. Provider 停用/缺配置、state 缺失/过期/不匹配、code 缺失或上游失败时拒绝，不创建 token 或绑定<br>2. 失败写登录/操作日志但不暴露 clientSecret、上游 token、内部堆栈或账号枚举信息 | 1. state 单次消费，OAuth account/user/token 关联与回调身份一致<br>2. 失败前后账号绑定数、用户权限和现有 token 保持不变 | 1. 接口允许匿名访问，但必须执行限流、状态校验或防枚举规则<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `environment` |
| API-240 | `GET /api/system/oauth/{provider}/redirect` | 1. 合法 Provider 和 state/code 完成 HTTP 302 跳转，Location 仅指向允许的本站或 Provider 地址<br>2. callback 按登录/绑定模式创建正确 token 或绑定关系，并记录 OAuth 登录方式和结果 | 1. Provider 停用/缺配置、state 缺失/过期/不匹配、code 缺失或上游失败时拒绝，不创建 token 或绑定<br>2. 失败写登录/操作日志但不暴露 clientSecret、上游 token、内部堆栈或账号枚举信息 | 1. state 单次消费，OAuth account/user/token 关联与回调身份一致<br>2. 失败前后账号绑定数、用户权限和现有 token 保持不变 | 1. 接口允许匿名访问，但必须执行限流、状态校验或防枚举规则<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `environment` |
| API-241 | `GET /api/system/oauth/provider` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-242 | `GET /api/system/profile/login-records` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-243 | `GET /api/system/profile/oauth/accounts` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-244 | `POST /api/system/login` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 缺少必填参数、参数过期或凭据错误时返回统一错误结构，不暴露账号、密钥或内部堆栈<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口允许匿名访问，但必须执行限流、状态校验或防枚举规则<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-245 | `POST /api/system/login/log/batch-delete` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-246 | `POST /api/system/logout` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-247 | `POST /api/system/oauth/provider` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-248 | `POST /api/system/oauth/provider/test` | 1. 合法临时参数或已保存资源完成真实连接/请求测试，并返回可读的耗时与结果<br>2. 测试不覆盖已保存密钥，不改变默认资源或业务数据，流式测试能够正常结束 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `environment` |
| API-249 | `POST /api/system/password-reset/confirm` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 缺少必填参数、参数过期或凭据错误时返回统一错误结构，不暴露账号、密钥或内部堆栈<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口允许匿名访问，但必须执行限流、状态校验或防枚举规则<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-250 | `POST /api/system/password-reset/request` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 缺少必填参数、参数过期或凭据错误时返回统一错误结构，不暴露账号、密钥或内部堆栈<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口允许匿名访问，但必须执行限流、状态校验或防枚举规则<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-251 | `POST /api/system/profile/oauth/{provider}/bind` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-252 | `PUT /api/system/oauth/provider/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-253 | `PUT /api/system/oauth/provider/status/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## mail / account

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-254 | `DELETE /api/system/mail/account/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-255 | `GET /api/system/mail/account` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-256 | `POST /api/system/mail/account` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-257 | `POST /api/system/mail/account/batch-delete` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-258 | `POST /api/system/mail/account/test` | 1. 合法临时参数或已保存资源完成真实连接/请求测试，并返回可读的耗时与结果<br>2. 测试不覆盖已保存密钥，不改变默认资源或业务数据，流式测试能够正常结束 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `environment` |
| API-259 | `PUT /api/system/mail/account/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-260 | `PUT /api/system/mail/account/default/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-261 | `PUT /api/system/mail/account/status/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## notice

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-262 | `DELETE /api/system/notice/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-263 | `GET /api/system/notice` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-264 | `GET /api/system/notice/{id}/read-stats` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-265 | `GET /api/system/notice/{id}/read-users` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-266 | `POST /api/system/notice` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-267 | `PUT /api/system/notice/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## online / user

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-268 | `DELETE /api/system/online/user/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-269 | `DELETE /api/system/online/user/expired` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-270 | `DELETE /api/system/online/user/user/{userId}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-271 | `GET /api/system/online/user` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## operation / log

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-272 | `DELETE /api/system/operation/log/clean` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-273 | `GET /api/system/operation/log` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-274 | `GET /api/system/operation/log/export` | 1. 合法请求返回文件或导出内容，文件名、Content-Type、长度和内容与目标记录/筛选一致<br>2. 文件读取支持合法 Range 时返回 206；下载包含 nosniff，危险文件不以内联可执行方式响应 | 1. 文件不存在或越权时返回 404/403；路径穿越被拒绝；非法 Range 返回 416 和正确 Content-Range<br>2. 存储读取失败时不返回截断但标记成功的文件，不泄漏本地绝对路径、Bucket 密钥或内部堆栈 | 1. 响应字节、文件大小、MIME、文件名和 Range 区间与存储元数据及源文件一致<br>2. 导出内容严格使用当前筛选和数据权限，危险文件响应头符合下载策略 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-275 | `GET /api/system/operation/log/stats` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## role

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-276 | `DELETE /api/system/role/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-277 | `GET /api/system/role` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-278 | `POST /api/system/role` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-279 | `PUT /api/system/role/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## rule

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-280 | `DELETE /api/system/rule/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-281 | `GET /api/system/rule` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-282 | `POST /api/system/rule` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-283 | `PUT /api/system/rule/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## sms / provider

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-284 | `DELETE /api/system/sms/provider/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-285 | `GET /api/system/sms/provider` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-286 | `POST /api/system/sms/provider` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-287 | `POST /api/system/sms/provider/batch-delete` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-288 | `POST /api/system/sms/provider/test` | 1. 合法临时参数或已保存资源完成真实连接/请求测试，并返回可读的耗时与结果<br>2. 测试不覆盖已保存密钥，不改变默认资源或业务数据，流式测试能够正常结束 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `environment` |
| API-289 | `PUT /api/system/sms/provider/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-290 | `PUT /api/system/sms/provider/default/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-291 | `PUT /api/system/sms/provider/status/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## sms / template

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-292 | `DELETE /api/system/sms/template/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-293 | `GET /api/system/sms/template` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-294 | `POST /api/system/sms/template` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-295 | `POST /api/system/sms/template/test` | 1. 合法临时参数或已保存资源完成真实连接/请求测试，并返回可读的耗时与结果<br>2. 测试不覆盖已保存密钥，不改变默认资源或业务数据，流式测试能够正常结束 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `environment` |
| API-296 | `PUT /api/system/sms/template/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-297 | `PUT /api/system/sms/template/status/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## storage

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-298 | `DELETE /api/system/storage/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-299 | `GET /api/system/storage` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-300 | `POST /api/system/storage` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-301 | `PUT /api/system/storage/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## user

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-302 | `DELETE /api/system/user/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-303 | `GET /api/system/user` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-304 | `POST /api/system/user` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-305 | `PUT /api/system/user/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## 生产诊断

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-306 | `GET /api/health` | 1. 依赖正常时返回约定状态码；health 返回 ok，ready/doctor 的检查项、总状态和时间戳完整<br>2. ready/doctor 的 env、database、migration、seed、存储、邮件和生产安全结论与真实环境一致 | 1. 数据库、迁移、关键 seed 或生产安全项失败时 ready/doctor 返回 503 和具体失败项；health 不伪装依赖状态<br>2. doctor 未登录返回 401、无权限返回 403；诊断输出不得泄漏数据库密码、密钥或完整凭据 | 1. 检查项名称、ready/warning/failed 状态和 HTTP 状态码映射稳定<br>2. 修改依赖状态后结果立即反映真实环境，不使用过期成功缓存 | 1. 接口允许匿名访问，但必须执行限流、状态校验或防枚举规则<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-307 | `GET /api/ready` | 1. 依赖正常时返回约定状态码；health 返回 ok，ready/doctor 的检查项、总状态和时间戳完整<br>2. ready/doctor 的 env、database、migration、seed、存储、邮件和生产安全结论与真实环境一致 | 1. 数据库、迁移、关键 seed 或生产安全项失败时 ready/doctor 返回 503 和具体失败项；health 不伪装依赖状态<br>2. doctor 未登录返回 401、无权限返回 403；诊断输出不得泄漏数据库密码、密钥或完整凭据 | 1. 检查项名称、ready/warning/failed 状态和 HTTP 状态码映射稳定<br>2. 修改依赖状态后结果立即反映真实环境，不使用过期成功缓存 | 1. 接口允许匿名访问，但必须执行限流、状态校验或防枚举规则<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-308 | `GET /api/system/doctor` | 1. 依赖正常时返回约定状态码；health 返回 ok，ready/doctor 的检查项、总状态和时间戳完整<br>2. ready/doctor 的 env、database、migration、seed、存储、邮件和生产安全结论与真实环境一致 | 1. 数据库、迁移、关键 seed 或生产安全项失败时 ready/doctor 返回 503 和具体失败项；health 不伪装依赖状态<br>2. doctor 未登录返回 401、无权限返回 403；诊断输出不得泄漏数据库密码、密钥或完整凭据 | 1. 检查项名称、ready/warning/failed 状态和 HTTP 状态码映射稳定<br>2. 修改依赖状态后结果立即反映真实环境，不使用过期成功缓存 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## 系统接口

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-309 | `GET /uploads/{path}` | 1. 合法请求返回文件或导出内容，文件名、Content-Type、长度和内容与目标记录/筛选一致<br>2. 文件读取支持合法 Range 时返回 206；下载包含 nosniff，危险文件不以内联可执行方式响应 | 1. 文件不存在或越权时返回 404/403；路径穿越被拒绝；非法 Range 返回 416 和正确 Content-Range<br>2. 存储读取失败时不返回截断但标记成功的文件，不泄漏本地绝对路径、Bucket 密钥或内部堆栈 | 1. 响应字节、文件大小、MIME、文件名和 Range 区间与存储元数据及源文件一致<br>2. 导出内容严格使用当前筛选和数据权限，危险文件响应头符合下载策略 | 1. 接口允许匿名访问，但必须执行限流、状态校验或防枚举规则<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## ai / workflow

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-310 | `GET /api/system/ai/workflow/definitions` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-311 | `GET /api/system/ai/workflow/visual/definitions` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-312 | `GET /api/system/ai/workflow/visual/definitions/{id}` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-313 | `GET /api/system/ai/workflow/runs` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-314 | `GET /api/system/ai/workflow/runs/{id}` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-315 | `GET /api/system/ai/workflow/runs/{id}/events` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-316 | `POST /api/system/ai/workflow/visual/versions` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-317 | `POST /api/system/ai/workflow/assets` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-318 | `POST /api/system/ai/workflow/visual/definitions/{id}/versions/{version}/publish` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-319 | `POST /api/system/ai/workflow/visual/definitions/{id}/runs` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-320 | `POST /api/system/ai/workflow/runs/{id}/waits/{waitId}/decision` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-321 | `POST /api/system/ai/workflow/runs/{id}/events` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-322 | `POST /api/system/ai/workflow/runs/{id}/resume` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-323 | `POST /api/system/ai/workflow/{code}/runs` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## ai / pricing

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-324 | `GET /api/system/ai/pricing/catalog/models` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-325 | `GET /api/system/ai/pricing/catalog/status` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-326 | `POST /api/system/ai/pricing/catalog/refresh` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## ai / setup

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-327 | `GET /api/system/ai/setup/summary` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-328 | `POST /api/system/ai/setup/complete` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-329 | `POST /api/system/ai/setup/discover` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## ai / playground

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-330 | `GET /api/system/ai/playground/options` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-331 | `GET /api/system/ai/playground/runtime-config/{usage}` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-332 | `POST /api/system/ai/playground/chat` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-333 | `POST /api/system/ai/playground/chat/stream` | 1. 合法输入产生可持续消费的流，meta/delta/tool/finish 或错误事件顺序符合协议<br>2. 正文增量、usage、finishReason 和最终持久化状态一致，客户端结束后不残留 loading | 1. 未登录返回 401；缺少对应 ability 返回 403，且不启动模型或外部请求<br>2. 参数非法、Provider 超时/断流、主动取消或工具失败时结束流并持久化准确状态，不重复消息、Step 或副作用 | 1. 每个事件可解析，delta 拼接结果等于完成文本，usage 和 finishReason 写入最终记录<br>2. 失败、取消和 length 截断状态可区分，Run/Step/消息不会长期停留 running | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `environment` |

## ai / runtime-config

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-334 | `GET /api/system/ai/runtime-config/{usage}` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## ai / runtime

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-335 | `GET /api/system/ai/runtime/purposes` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-336 | `GET /api/system/ai/runtime/provider-health` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-337 | `GET /api/system/ai/runtime/invocations` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-338 | `GET /api/system/ai/runtime/invocations/{id}` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-339 | `PUT /api/system/ai/runtime/purposes/{purpose}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## dashboard / summary

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-340 | `GET /api/system/dashboard/summary` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## dept / tree

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-341 | `GET /api/system/dept/tree` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## dept / users

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-342 | `GET /api/system/dept/users/{id}` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## file

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-343 | `GET /api/system/file/{id}/references` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## info

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-344 | `GET /api/system/info` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## menu

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-345 | `GET /api/system/menu` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## module / generator

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-346 | `GET /api/system/module/generator/drafts` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-347 | `GET /api/system/module/generator/drafts/{name}/diff` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-348 | `GET /api/system/module/generator/capabilities` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-349 | `GET /api/system/module/generator/example` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-350 | `GET /api/system/module/generator/schema` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-351 | `POST /api/system/module/generator/generate` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-352 | `POST /api/system/module/generator/publish` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-353 | `POST /api/system/module/generator/rollback` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## notice / my

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-354 | `GET /api/system/notice/my` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-355 | `GET /api/system/notice/my/unread-count` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-356 | `POST /api/system/notice/my/{id}/read` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-357 | `POST /api/system/notice/my/read-all` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## profile

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-358 | `GET /api/system/profile` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-359 | `PUT /api/system/profile` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## role / deptTree

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-360 | `GET /api/system/role/deptTree` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## role / ruleList

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-361 | `GET /api/system/role/ruleList` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## role / users

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-362 | `GET /api/system/role/users/{id}` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## rule / parent

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-363 | `GET /api/system/rule/parent` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## rule / tree

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-364 | `GET /api/system/rule/tree` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## settings / config

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-365 | `GET /api/system/settings/config/items` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-366 | `PUT /api/system/settings/config/save` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## user / dept

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-367 | `GET /api/system/user/dept` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## user / role

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-368 | `GET /api/system/user/role` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## ai / approval

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-369 | `POST /api/system/ai/approval/{id}/decision` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## dept / batch-delete

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-370 | `POST /api/system/dept/batch-delete` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## notice / batch-delete

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-371 | `POST /api/system/notice/batch-delete` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## profile / avatar

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-372 | `POST /api/system/profile/avatar` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## role / batch-delete

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-373 | `POST /api/system/role/batch-delete` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## role / copy

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-374 | `POST /api/system/role/copy/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## role / setRule

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-375 | `POST /api/system/role/setRule` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## rule / batch-delete

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-376 | `POST /api/system/rule/batch-delete` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## storage / batch-delete

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-377 | `POST /api/system/storage/batch-delete` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## storage / test

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-378 | `POST /api/system/storage/test` | 1. 合法临时参数或已保存资源完成真实连接/请求测试，并返回可读的耗时与结果<br>2. 测试不覆盖已保存密钥，不改变默认资源或业务数据，流式测试能够正常结束 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `environment` |

## user / batch-delete

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-379 | `POST /api/system/user/batch-delete` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## notice / publish

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-380 | `PUT /api/system/notice/publish/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## notice / revoke

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-381 | `PUT /api/system/notice/revoke/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## profile / password

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-382 | `PUT /api/system/profile/password` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## role / status

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-383 | `PUT /api/system/role/status/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## rule / hidden

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-384 | `PUT /api/system/rule/hidden/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## rule / status

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-385 | `PUT /api/system/rule/status/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## storage / default

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-386 | `PUT /api/system/storage/default/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## storage / status

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-387 | `PUT /api/system/storage/status/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## user / resetPassword

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-388 | `PUT /api/system/user/resetPassword` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## 执行记录

每次执行至少记录版本、环境、用例 ID、结果、requestId 或日志证据。结果只允许 `Pass`、`Fail`、`Blocked`、`Skipped`。P0 业务用例或安全接口存在 `Fail` 时禁止发布。
