# Admin Base API 测试用例矩阵

> 该文件由 `pnpm test:docs` 根据 `tests/coverage/api-test-cases.ts` 生成。请修改机器可读清单，不要直接维护本文件。

## 1. 范围与判定

- 当前登记 API 操作：**242** 个。
- 覆盖状态：`automated` 183，`planned` 43，`environment` 16。
- 成功不仅指 HTTP 2xx，还必须同时满足响应契约、数据事实、权限范围、副作用和审计要求。
- 失败不仅指返回错误，还必须验证无越权、无敏感信息泄漏、无部分写入、可追踪且可以恢复或重试。
- `automated` 表示已有自动化覆盖该模块主路径，不等于该操作的每个分支均已自动化；逐接口自动化仍按风险递增补齐。
- `environment` 表示需要真实 SMTP、S3、OAuth、SMS 或 AI Provider；`manual` 表示当前主要依赖手工验收；`planned` 表示用例已登记但自动化尚未落地。

## ai / agent

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-001 | `DELETE /api/system/ai/agent/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-002 | `GET /api/system/ai/agent` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-003 | `GET /api/system/ai/agent/options` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-004 | `GET /api/system/ai/agent/runs` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-005 | `GET /api/system/ai/agent/runs/{id}/steps` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-006 | `POST /api/system/ai/agent` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-007 | `PUT /api/system/ai/agent/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## ai / chat

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-008 | `DELETE /api/system/ai/chat/sessions/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-009 | `GET /api/system/ai/chat/options` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-010 | `GET /api/system/ai/chat/runtime-config` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-011 | `GET /api/system/ai/chat/sessions` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-012 | `GET /api/system/ai/chat/sessions/{id}/approvals` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-013 | `GET /api/system/ai/chat/sessions/{id}/export` | 1. 合法请求返回文件或导出内容，文件名、Content-Type、长度和内容与目标记录/筛选一致<br>2. 文件读取支持合法 Range 时返回 206；下载包含 nosniff，危险文件不以内联可执行方式响应 | 1. 文件不存在或越权时返回 404/403；路径穿越被拒绝；非法 Range 返回 416 和正确 Content-Range<br>2. 存储读取失败时不返回截断但标记成功的文件，不泄漏本地绝对路径、Bucket 密钥或内部堆栈 | 1. 响应字节、文件大小、MIME、文件名和 Range 区间与存储元数据及源文件一致<br>2. 导出内容严格使用当前筛选和数据权限，危险文件响应头符合下载策略 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-014 | `GET /api/system/ai/chat/sessions/{id}/messages` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-015 | `GET /api/system/ai/chat/sessions/{id}/run/latest` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-016 | `POST /api/system/ai/chat/sessions/{sessionId}/client-actions/{id}/result` | 1. granted 结果在浏览器和服务端分别粗化后写入 Approval/Step，Run 进入 waiting_continuation 并恢复同一 Agent 流程<br>2. denied、unavailable 或 error 作为合法 Client Tool 结果完成当前 Step，Agent 随后询问城市而不是把会话标记失败 | 1. 未登录返回 401，缺少 system.aiChat.chat 返回 403；跨用户、跨会话、非 browser-location Tool 返回 409<br>2. 过期或已处理结果返回 409，重复提交不能创建第二条消息、执行第二次续跑或覆盖原结果 | 1. 经纬度只保留两位小数，accuracy 为受限安全整数；Approval、Step 和系统上下文不含客户端提交的精确坐标<br>2. Approval 为 executed、Step 为 completed、父 Run 为 waiting_continuation；过期请求将 Step/Run 收口为 denied/stopped | 1. 接口只要求当前会话用户的 system.aiChat.chat，不使用管理员 system.aiAgent.approve 权限，也不能执行任意服务端 Tool<br>2. 请求体使用判别联合严格校验；服务端只构造受控输出和系统消息，不接受客户端自定义 output 或日志字段 | 1. 成功或降级结果刷新会话消息统计并允许一次续跑；过期结果停止等待中的 Run，避免永久挂起<br>2. 操作日志只记录 browser_location、结果状态和是否授权，不记录经纬度、accuracy 或浏览器原始错误对象 | `automated` |
| API-017 | `POST /api/system/ai/chat/sessions` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-018 | `POST /api/system/ai/chat/sessions/{id}/messages/{messageId}/regenerate` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-019 | `POST /api/system/ai/chat/sessions/{id}/messages/stream` | 1. 合法输入产生可持续消费的流，meta/delta/tool/finish 或错误事件顺序符合协议<br>2. 正文增量、usage、finishReason 和最终持久化状态一致，客户端结束后不残留 loading | 1. 未登录返回 401；缺少对应 ability 返回 403，且不启动模型或外部请求<br>2. 参数非法、Provider 超时/断流、主动取消或工具失败时结束流并持久化准确状态，不重复消息、Step 或副作用 | 1. 每个事件可解析，delta 拼接结果等于完成文本，usage 和 finishReason 写入最终记录<br>2. 失败、取消和 length 截断状态可区分，Run/Step/消息不会长期停留 running | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `environment` |
| API-020 | `PUT /api/system/ai/chat/sessions/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## ai / model

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-021 | `DELETE /api/system/ai/model/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-022 | `GET /api/system/ai/model` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-023 | `POST /api/system/ai/model` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-024 | `POST /api/system/ai/model/test` | 1. 合法临时参数或已保存资源完成真实连接/请求测试，并返回可读的耗时与结果<br>2. 测试不覆盖已保存密钥，不改变默认资源或业务数据，流式测试能够正常结束 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `environment` |
| API-025 | `POST /api/system/ai/model/test/stream` | 1. 合法输入产生可持续消费的流，meta/delta/tool/finish 或错误事件顺序符合协议<br>2. 正文增量、usage、finishReason 和最终持久化状态一致，客户端结束后不残留 loading | 1. 未登录返回 401；缺少对应 ability 返回 403，且不启动模型或外部请求<br>2. 参数非法、Provider 超时/断流、主动取消或工具失败时结束流并持久化准确状态，不重复消息、Step 或副作用 | 1. 每个事件可解析，delta 拼接结果等于完成文本，usage 和 finishReason 写入最终记录<br>2. 失败、取消和 length 截断状态可区分，Run/Step/消息不会长期停留 running | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `environment` |
| API-026 | `PUT /api/system/ai/model/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-027 | `PUT /api/system/ai/model/default/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-028 | `PUT /api/system/ai/model/status/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## ai / provider

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-029 | `DELETE /api/system/ai/provider/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-030 | `GET /api/system/ai/provider/{id}/models` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-031 | `GET /api/system/ai/provider/{id}/test-models` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `environment` |
| API-032 | `GET /api/system/ai/provider` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-033 | `POST /api/system/ai/provider` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-034 | `POST /api/system/ai/provider/batch-delete` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-035 | `POST /api/system/ai/provider/test` | 1. 合法临时参数或已保存资源完成真实连接/请求测试，并返回可读的耗时与结果<br>2. 测试不覆盖已保存密钥，不改变默认资源或业务数据，流式测试能够正常结束 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `environment` |
| API-036 | `POST /api/system/ai/provider/test/stream` | 1. 合法输入产生可持续消费的流，meta/delta/tool/finish 或错误事件顺序符合协议<br>2. 正文增量、usage、finishReason 和最终持久化状态一致，客户端结束后不残留 loading | 1. 未登录返回 401；缺少对应 ability 返回 403，且不启动模型或外部请求<br>2. 参数非法、Provider 超时/断流、主动取消或工具失败时结束流并持久化准确状态，不重复消息、Step 或副作用 | 1. 每个事件可解析，delta 拼接结果等于完成文本，usage 和 finishReason 写入最终记录<br>2. 失败、取消和 length 截断状态可区分，Run/Step/消息不会长期停留 running | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `environment` |
| API-037 | `PUT /api/system/ai/provider/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-038 | `PUT /api/system/ai/provider/default/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-039 | `PUT /api/system/ai/provider/status/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## ai / web-search

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-040 | `DELETE /api/system/ai/web-search/provider/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-041 | `GET /api/system/ai/web-search/provider` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-042 | `POST /api/system/ai/web-search/provider` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-043 | `POST /api/system/ai/web-search/provider/batch-delete` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-044 | `POST /api/system/ai/web-search/provider/test` | 1. 合法临时参数或已保存资源完成真实连接/请求测试，并返回可读的耗时与结果<br>2. 测试不覆盖已保存密钥，不改变默认资源或业务数据，流式测试能够正常结束 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `environment` |
| API-045 | `PUT /api/system/ai/web-search/provider/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-046 | `PUT /api/system/ai/web-search/provider/status/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## ai / tool

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-047 | `DELETE /api/system/ai/tool/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-048 | `GET /api/system/ai/tool` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-049 | `POST /api/system/ai/tool` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-050 | `PUT /api/system/ai/tool/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## config / group

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-051 | `DELETE /api/system/config/group/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-052 | `GET /api/system/config/group` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-053 | `POST /api/system/config/group` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-054 | `POST /api/system/config/group/batch-delete` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-055 | `PUT /api/system/config/group/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## config / items

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-056 | `DELETE /api/system/config/items/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-057 | `GET /api/system/config/items` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-058 | `POST /api/system/config/items` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-059 | `POST /api/system/config/items/batch-delete` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-060 | `POST /api/system/config/items/refreshCache` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-061 | `PUT /api/system/config/items/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-062 | `PUT /api/system/config/items/save` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## dept

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-063 | `DELETE /api/system/dept/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-064 | `GET /api/system/dept` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-065 | `POST /api/system/dept` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-066 | `PUT /api/system/dept/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## dict / item

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-067 | `DELETE /api/system/dict/item/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-068 | `GET /api/system/dict/item` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-069 | `POST /api/system/dict/item` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-070 | `POST /api/system/dict/item/batch-delete` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-071 | `PUT /api/system/dict/item/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## dict / list

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-072 | `DELETE /api/system/dict/list/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-073 | `GET /api/system/dict/list` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-074 | `GET /api/system/dict/list/all` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-075 | `POST /api/system/dict/list` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-076 | `POST /api/system/dict/list/batch-delete` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-077 | `PUT /api/system/dict/list/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## file / chunk

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-078 | `DELETE /api/system/file/chunk/{uploadId}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-079 | `DELETE /api/system/file/chunk/clean-expired` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-080 | `POST /api/system/file/chunk/complete` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-081 | `POST /api/system/file/chunk/init` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-082 | `POST /api/system/file/chunk/part` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## file / group

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-083 | `DELETE /api/system/file/group/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-084 | `GET /api/system/file/group/tree` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-085 | `POST /api/system/file/group` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-086 | `PUT /api/system/file/group/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## file / list

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-087 | `DELETE /api/system/file/list/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-088 | `DELETE /api/system/file/list/clean-trash` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-089 | `DELETE /api/system/file/list/force/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-090 | `GET /api/system/file/list` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-091 | `GET /api/system/file/list/download/{id}` | 1. 合法请求返回文件或导出内容，文件名、Content-Type、长度和内容与目标记录/筛选一致<br>2. 文件读取支持合法 Range 时返回 206；下载包含 nosniff，危险文件不以内联可执行方式响应 | 1. 文件不存在或越权时返回 404/403；路径穿越被拒绝；非法 Range 返回 416 和正确 Content-Range<br>2. 存储读取失败时不返回截断但标记成功的文件，不泄漏本地绝对路径、Bucket 密钥或内部堆栈 | 1. 响应字节、文件大小、MIME、文件名和 Range 区间与存储元数据及源文件一致<br>2. 导出内容严格使用当前筛选和数据权限，危险文件响应头符合下载策略 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `environment` |
| API-092 | `GET /api/system/file/list/trash` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-093 | `POST /api/system/file/list/batch-delete` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-094 | `POST /api/system/file/list/batch-force` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-095 | `POST /api/system/file/list/batch-restore` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-096 | `POST /api/system/file/list/copy` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-097 | `POST /api/system/file/list/upload` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-098 | `PUT /api/system/file/list/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-099 | `PUT /api/system/file/list/move` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-100 | `PUT /api/system/file/list/rename/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-101 | `PUT /api/system/file/list/restore/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## file / reference

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-102 | `DELETE /api/system/file/reference` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-103 | `POST /api/system/file/reference` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## 认证安全

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-104 | `DELETE /api/system/login/log/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-105 | `DELETE /api/system/login/log/clean` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-106 | `DELETE /api/system/oauth/provider/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-107 | `DELETE /api/system/profile/oauth/{provider}/unbind` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-108 | `GET /api/system/login/captcha` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 缺少必填参数、参数过期或凭据错误时返回统一错误结构，不暴露账号、密钥或内部堆栈<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口允许匿名访问，但必须执行限流、状态校验或防枚举规则<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-109 | `GET /api/system/login/log` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-110 | `GET /api/system/login/options` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 缺少必填参数、参数过期或凭据错误时返回统一错误结构，不暴露账号、密钥或内部堆栈<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口允许匿名访问，但必须执行限流、状态校验或防枚举规则<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-111 | `GET /api/system/oauth/{provider}/callback` | 1. 合法 Provider 和 state/code 完成 HTTP 302 跳转，Location 仅指向允许的本站或 Provider 地址<br>2. callback 按登录/绑定模式创建正确 token 或绑定关系，并记录 OAuth 登录方式和结果 | 1. Provider 停用/缺配置、state 缺失/过期/不匹配、code 缺失或上游失败时拒绝，不创建 token 或绑定<br>2. 失败写登录/操作日志但不暴露 clientSecret、上游 token、内部堆栈或账号枚举信息 | 1. state 单次消费，OAuth account/user/token 关联与回调身份一致<br>2. 失败前后账号绑定数、用户权限和现有 token 保持不变 | 1. 接口允许匿名访问，但必须执行限流、状态校验或防枚举规则<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `environment` |
| API-112 | `GET /api/system/oauth/{provider}/redirect` | 1. 合法 Provider 和 state/code 完成 HTTP 302 跳转，Location 仅指向允许的本站或 Provider 地址<br>2. callback 按登录/绑定模式创建正确 token 或绑定关系，并记录 OAuth 登录方式和结果 | 1. Provider 停用/缺配置、state 缺失/过期/不匹配、code 缺失或上游失败时拒绝，不创建 token 或绑定<br>2. 失败写登录/操作日志但不暴露 clientSecret、上游 token、内部堆栈或账号枚举信息 | 1. state 单次消费，OAuth account/user/token 关联与回调身份一致<br>2. 失败前后账号绑定数、用户权限和现有 token 保持不变 | 1. 接口允许匿名访问，但必须执行限流、状态校验或防枚举规则<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `environment` |
| API-113 | `GET /api/system/oauth/provider` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-114 | `GET /api/system/profile/login-records` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-115 | `GET /api/system/profile/oauth/accounts` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-116 | `POST /api/system/login` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 缺少必填参数、参数过期或凭据错误时返回统一错误结构，不暴露账号、密钥或内部堆栈<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口允许匿名访问，但必须执行限流、状态校验或防枚举规则<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-117 | `POST /api/system/login/log/batch-delete` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-118 | `POST /api/system/logout` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-119 | `POST /api/system/oauth/provider` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-120 | `POST /api/system/oauth/provider/test` | 1. 合法临时参数或已保存资源完成真实连接/请求测试，并返回可读的耗时与结果<br>2. 测试不覆盖已保存密钥，不改变默认资源或业务数据，流式测试能够正常结束 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `environment` |
| API-121 | `POST /api/system/password-reset/confirm` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 缺少必填参数、参数过期或凭据错误时返回统一错误结构，不暴露账号、密钥或内部堆栈<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口允许匿名访问，但必须执行限流、状态校验或防枚举规则<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-122 | `POST /api/system/password-reset/request` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 缺少必填参数、参数过期或凭据错误时返回统一错误结构，不暴露账号、密钥或内部堆栈<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口允许匿名访问，但必须执行限流、状态校验或防枚举规则<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-123 | `POST /api/system/profile/oauth/{provider}/bind` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-124 | `PUT /api/system/oauth/provider/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-125 | `PUT /api/system/oauth/provider/status/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## mail / account

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-126 | `DELETE /api/system/mail/account/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-127 | `GET /api/system/mail/account` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-128 | `POST /api/system/mail/account` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-129 | `POST /api/system/mail/account/batch-delete` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-130 | `POST /api/system/mail/account/test` | 1. 合法临时参数或已保存资源完成真实连接/请求测试，并返回可读的耗时与结果<br>2. 测试不覆盖已保存密钥，不改变默认资源或业务数据，流式测试能够正常结束 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `environment` |
| API-131 | `PUT /api/system/mail/account/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-132 | `PUT /api/system/mail/account/default/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-133 | `PUT /api/system/mail/account/status/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## notice

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-134 | `DELETE /api/system/notice/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-135 | `GET /api/system/notice` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-136 | `GET /api/system/notice/{id}/read-stats` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-137 | `GET /api/system/notice/{id}/read-users` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-138 | `POST /api/system/notice` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-139 | `PUT /api/system/notice/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## online / user

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-140 | `DELETE /api/system/online/user/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-141 | `DELETE /api/system/online/user/expired` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-142 | `DELETE /api/system/online/user/user/{userId}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-143 | `GET /api/system/online/user` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## operation / log

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-144 | `DELETE /api/system/operation/log/clean` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-145 | `GET /api/system/operation/log` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-146 | `GET /api/system/operation/log/export` | 1. 合法请求返回文件或导出内容，文件名、Content-Type、长度和内容与目标记录/筛选一致<br>2. 文件读取支持合法 Range 时返回 206；下载包含 nosniff，危险文件不以内联可执行方式响应 | 1. 文件不存在或越权时返回 404/403；路径穿越被拒绝；非法 Range 返回 416 和正确 Content-Range<br>2. 存储读取失败时不返回截断但标记成功的文件，不泄漏本地绝对路径、Bucket 密钥或内部堆栈 | 1. 响应字节、文件大小、MIME、文件名和 Range 区间与存储元数据及源文件一致<br>2. 导出内容严格使用当前筛选和数据权限，危险文件响应头符合下载策略 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-147 | `GET /api/system/operation/log/stats` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## role

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-148 | `DELETE /api/system/role/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-149 | `GET /api/system/role` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-150 | `POST /api/system/role` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-151 | `PUT /api/system/role/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## rule

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-152 | `DELETE /api/system/rule/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-153 | `GET /api/system/rule` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-154 | `POST /api/system/rule` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-155 | `PUT /api/system/rule/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## sms / provider

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-156 | `DELETE /api/system/sms/provider/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-157 | `GET /api/system/sms/provider` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-158 | `POST /api/system/sms/provider` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-159 | `POST /api/system/sms/provider/batch-delete` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-160 | `POST /api/system/sms/provider/test` | 1. 合法临时参数或已保存资源完成真实连接/请求测试，并返回可读的耗时与结果<br>2. 测试不覆盖已保存密钥，不改变默认资源或业务数据，流式测试能够正常结束 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `environment` |
| API-161 | `PUT /api/system/sms/provider/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-162 | `PUT /api/system/sms/provider/default/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-163 | `PUT /api/system/sms/provider/status/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## sms / template

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-164 | `DELETE /api/system/sms/template/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-165 | `GET /api/system/sms/template` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-166 | `POST /api/system/sms/template` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-167 | `POST /api/system/sms/template/test` | 1. 合法临时参数或已保存资源完成真实连接/请求测试，并返回可读的耗时与结果<br>2. 测试不覆盖已保存密钥，不改变默认资源或业务数据，流式测试能够正常结束 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `environment` |
| API-168 | `PUT /api/system/sms/template/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-169 | `PUT /api/system/sms/template/status/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## storage

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-170 | `DELETE /api/system/storage/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-171 | `GET /api/system/storage` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-172 | `POST /api/system/storage` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |
| API-173 | `PUT /api/system/storage/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## user

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-174 | `DELETE /api/system/user/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-175 | `GET /api/system/user` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-176 | `POST /api/system/user` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-177 | `PUT /api/system/user/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## 生产诊断

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-178 | `GET /api/health` | 1. 依赖正常时返回约定状态码；health 返回 ok，ready/doctor 的检查项、总状态和时间戳完整<br>2. ready/doctor 的 env、database、migration、seed、存储、邮件和生产安全结论与真实环境一致 | 1. 数据库、迁移、关键 seed 或生产安全项失败时 ready/doctor 返回 503 和具体失败项；health 不伪装依赖状态<br>2. doctor 未登录返回 401、无权限返回 403；诊断输出不得泄漏数据库密码、密钥或完整凭据 | 1. 检查项名称、ready/warning/failed 状态和 HTTP 状态码映射稳定<br>2. 修改依赖状态后结果立即反映真实环境，不使用过期成功缓存 | 1. 接口允许匿名访问，但必须执行限流、状态校验或防枚举规则<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-179 | `GET /api/ready` | 1. 依赖正常时返回约定状态码；health 返回 ok，ready/doctor 的检查项、总状态和时间戳完整<br>2. ready/doctor 的 env、database、migration、seed、存储、邮件和生产安全结论与真实环境一致 | 1. 数据库、迁移、关键 seed 或生产安全项失败时 ready/doctor 返回 503 和具体失败项；health 不伪装依赖状态<br>2. doctor 未登录返回 401、无权限返回 403；诊断输出不得泄漏数据库密码、密钥或完整凭据 | 1. 检查项名称、ready/warning/failed 状态和 HTTP 状态码映射稳定<br>2. 修改依赖状态后结果立即反映真实环境，不使用过期成功缓存 | 1. 接口允许匿名访问，但必须执行限流、状态校验或防枚举规则<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-180 | `GET /api/system/doctor` | 1. 依赖正常时返回约定状态码；health 返回 ok，ready/doctor 的检查项、总状态和时间戳完整<br>2. ready/doctor 的 env、database、migration、seed、存储、邮件和生产安全结论与真实环境一致 | 1. 数据库、迁移、关键 seed 或生产安全项失败时 ready/doctor 返回 503 和具体失败项；health 不伪装依赖状态<br>2. doctor 未登录返回 401、无权限返回 403；诊断输出不得泄漏数据库密码、密钥或完整凭据 | 1. 检查项名称、ready/warning/failed 状态和 HTTP 状态码映射稳定<br>2. 修改依赖状态后结果立即反映真实环境，不使用过期成功缓存 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## 系统接口

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-181 | `GET /uploads/{path}` | 1. 合法请求返回文件或导出内容，文件名、Content-Type、长度和内容与目标记录/筛选一致<br>2. 文件读取支持合法 Range 时返回 206；下载包含 nosniff，危险文件不以内联可执行方式响应 | 1. 文件不存在或越权时返回 404/403；路径穿越被拒绝；非法 Range 返回 416 和正确 Content-Range<br>2. 存储读取失败时不返回截断但标记成功的文件，不泄漏本地绝对路径、Bucket 密钥或内部堆栈 | 1. 响应字节、文件大小、MIME、文件名和 Range 区间与存储元数据及源文件一致<br>2. 导出内容严格使用当前筛选和数据权限，危险文件响应头符合下载策略 | 1. 接口允许匿名访问，但必须执行限流、状态校验或防枚举规则<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## ai / workflow

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-182 | `GET /api/system/ai/workflow/definitions` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-183 | `GET /api/system/ai/workflow/runs` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-184 | `GET /api/system/ai/workflow/runs/{id}` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-185 | `POST /api/system/ai/workflow/{code}/runs` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## ai / setup

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-186 | `GET /api/system/ai/setup/summary` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-187 | `POST /api/system/ai/setup/complete` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-188 | `POST /api/system/ai/setup/discover` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## ai / playground

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-189 | `GET /api/system/ai/playground/options` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-190 | `GET /api/system/ai/playground/runtime-config/{usage}` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-191 | `POST /api/system/ai/playground/chat` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-192 | `POST /api/system/ai/playground/chat/stream` | 1. 合法输入产生可持续消费的流，meta/delta/tool/finish 或错误事件顺序符合协议<br>2. 正文增量、usage、finishReason 和最终持久化状态一致，客户端结束后不残留 loading | 1. 未登录返回 401；缺少对应 ability 返回 403，且不启动模型或外部请求<br>2. 参数非法、Provider 超时/断流、主动取消或工具失败时结束流并持久化准确状态，不重复消息、Step 或副作用 | 1. 每个事件可解析，delta 拼接结果等于完成文本，usage 和 finishReason 写入最终记录<br>2. 失败、取消和 length 截断状态可区分，Run/Step/消息不会长期停留 running | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `environment` |

## ai / runtime-config

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-193 | `GET /api/system/ai/runtime-config/{usage}` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## dashboard / summary

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-194 | `GET /api/system/dashboard/summary` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## dept / tree

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-195 | `GET /api/system/dept/tree` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## dept / users

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-196 | `GET /api/system/dept/users/{id}` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## file

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-197 | `GET /api/system/file/{id}/references` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## info

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-198 | `GET /api/system/info` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## menu

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-199 | `GET /api/system/menu` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## module / generator

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-200 | `GET /api/system/module/generator/drafts` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-201 | `GET /api/system/module/generator/drafts/{name}/diff` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-202 | `GET /api/system/module/generator/capabilities` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-203 | `GET /api/system/module/generator/example` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-204 | `GET /api/system/module/generator/schema` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-205 | `POST /api/system/module/generator/generate` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-206 | `POST /api/system/module/generator/publish` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-207 | `POST /api/system/module/generator/rollback` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## notice / my

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-208 | `GET /api/system/notice/my` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-209 | `GET /api/system/notice/my/unread-count` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-210 | `POST /api/system/notice/my/{id}/read` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-211 | `POST /api/system/notice/my/read-all` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## profile

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-212 | `GET /api/system/profile` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-213 | `PUT /api/system/profile` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## role / deptTree

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-214 | `GET /api/system/role/deptTree` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## role / ruleList

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-215 | `GET /api/system/role/ruleList` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## role / users

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-216 | `GET /api/system/role/users/{id}` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## rule / parent

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-217 | `GET /api/system/rule/parent` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## rule / tree

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-218 | `GET /api/system/rule/tree` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## settings / config

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-219 | `GET /api/system/settings/config/items` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |
| API-220 | `PUT /api/system/settings/config/save` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## user / dept

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-221 | `GET /api/system/user/dept` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## user / role

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-222 | `GET /api/system/user/role` | 1. 合法查询返回 HTTP 2xx、统一成功结构和与当前筛选/权限一致的数据<br>2. 分页、筛选、空结果或详情字段符合接口语义，不泄漏未授权记录 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 返回记录、总数、排序、过滤和数据权限范围与数据库事实一致 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 普通查询不修改业务数据；读取回执、最近活跃等显式例外必须可追踪<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## ai / approval

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-223 | `POST /api/system/ai/approval/{id}/decision` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## dept / batch-delete

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-224 | `POST /api/system/dept/batch-delete` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## notice / batch-delete

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-225 | `POST /api/system/notice/batch-delete` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## profile / avatar

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-226 | `POST /api/system/profile/avatar` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## role / batch-delete

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-227 | `POST /api/system/role/batch-delete` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## role / copy

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-228 | `POST /api/system/role/copy/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## role / setRule

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-229 | `POST /api/system/role/setRule` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## rule / batch-delete

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-230 | `POST /api/system/rule/batch-delete` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## storage / batch-delete

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-231 | `POST /api/system/storage/batch-delete` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## storage / test

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-232 | `POST /api/system/storage/test` | 1. 合法临时参数或已保存资源完成真实连接/请求测试，并返回可读的耗时与结果<br>2. 测试不覆盖已保存密钥，不改变默认资源或业务数据，流式测试能够正常结束 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `environment` |

## user / batch-delete

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-233 | `POST /api/system/user/batch-delete` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## notice / publish

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-234 | `PUT /api/system/notice/publish/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## notice / revoke

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-235 | `PUT /api/system/notice/revoke/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## profile / password

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-236 | `PUT /api/system/profile/password` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## role / status

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-237 | `PUT /api/system/role/status/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## rule / hidden

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-238 | `PUT /api/system/rule/hidden/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## rule / status

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-239 | `PUT /api/system/rule/status/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## storage / default

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-240 | `PUT /api/system/storage/default/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## storage / status

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-241 | `PUT /api/system/storage/status/{id}` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 路径参数不存在、格式非法或目标越权时失败，不能误操作其他记录 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `planned` |

## user / resetPassword

| ID | Operation | 成功标准 | 失败标准 | 数据断言 | 安全要求 | 副作用/审计 | 覆盖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| API-242 | `PUT /api/system/user/resetPassword` | 1. 合法输入返回 HTTP 2xx 和统一成功结构，目标业务动作只执行一次<br>2. 数据库状态、缓存或外部资源状态与响应一致，刷新后仍可观察到结果 | 1. 未登录返回 401；缺少对应 ability 返回 403，且不产生业务写入<br>2. 非法查询或请求体、业务约束冲突和依赖失败时返回明确错误，不能产生半完成状态 | 1. 响应 code、message、data 或分页字段符合统一响应契约<br>2. 成功时目标记录和关联关系正确；失败时事务回滚且原数据保持不变 | 1. 接口要求有效 token 和声明的最小权限<br>2. 响应、日志和导出内容不得包含 password、token、secret、accessKey 或 clientSecret 明文 | 1. 写动作按风险要求写入操作日志，并记录 requestId、用户、动作和成功/失败状态<br>2. 缓存、token、文件、邮件、OAuth 或 AI 调用等副作用必须与事务结果一致且可重试 | `automated` |

## 执行记录

每次执行至少记录版本、环境、用例 ID、结果、requestId 或日志证据。结果只允许 `Pass`、`Fail`、`Blocked`、`Skipped`。P0 业务用例或安全接口存在 `Fail` 时禁止发布。
